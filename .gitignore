# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (can opt-in for committing if needed)
.env*

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# Phase 3.3 — generated data-refresh snapshots (raw + canonical). These are
# refreshed via `npm run refresh:data`, not committed source — the original
# bundled Toronto dataset at data/toronto-open-data/ is unaffected and
# stays tracked as-is. See docs/PHASE_3_3_DATA_REFRESH_SNAPSHOT_PIPELINE.md.
/data/raw/
/data/canonical/
