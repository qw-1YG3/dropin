# Launch Readiness Plan

This document records the launch-readiness decisions made while DropIn moves from a functional search product toward a public-facing production product. **It is a planning/decision record, not an implementation.** None of the items below have been built yet unless explicitly marked otherwise; each will get its own dedicated phase.

Four areas must be addressed before public launch: About, Feedback, Privacy/Disclaimer, and Production Security & Deployment Readiness. This document also re-affirms two standing decisions (Map View stays deferred; mobile stays a release requirement) so they survive future phases without needing to be re-litigated.

---

## 1. About DropIn

**Status: exists, needs a pre-launch UX/content review.** Current implementation: the "About DropIn" `Sheet`, opened from the header info icon (`app/page.tsx`, `infoSheetOpen`), already covers most of the required ground — what DropIn is, where data comes from, and a "Data sources" line naming the currently-covered municipalities (`AVAILABLE_MUNICIPALITIES_LABEL`, derived from the live registry, not hardcoded — see `lib/dropin/municipalities.ts`). See §19 for the Mobile Sheet System's confirmed information-architecture decision for this surface (stays a Sheet, not a dedicated route).

Before launch, the About content should be revised to clearly and concisely answer:

- **A. What is DropIn?** DropIn helps people quickly discover drop-in recreation activities across participating GTA municipalities without having to search through multiple municipal recreation websites.
- **B. Where does the information come from?** Activity information comes from official municipal recreation sources; DropIn reorganizes and presents it to make activities easier to search, compare, and discover.
- **C. How current is the information?** DropIn refreshes its data regularly, but municipal schedules, availability, prices, registration requirements, and other details may change. Users should be encouraged to check the Official Listing when appropriate before heading out.
- **D. Is DropIn an official municipal service?** No — DropIn is an independent project, not affiliated with or endorsed by the municipalities whose recreation information it helps users discover.

**Product principle**: the About copy should reinforce the data-transparency system already built into the product — Updated/freshness labels (`lastUpdatedLabel`, `daysAgoLabel`), the Official Listing CTA (`officialActionLabel`, Phase 3.5C), source attribution (`officialSource` shown in the Decision Sheet), honest handling of missing information ("never a guess, never a filler line," per `ageRestrictionLabel`/`attendanceRequirementLabel`'s own established discipline), and preservation of source truth (§6 below). Avoid long legalistic language — the experience should feel transparent and trustworthy, not defensive.

## 2. Feedback

**Status: exists as UI only — confirmed by direct inspection, not yet actually wired to anything.** The Feedback flow inside the About sheet (`feedbackStage`: `"idle" → "writing" → "sent"`) collects free text in a `<textarea>` and, on Send, transitions straight to a "Thanks — we've received your note and will take a look" confirmation. **A repo-wide check found no `fetch`/API call anywhere in the Send path** — the text is held in local component state and discarded on close; nothing is transmitted, logged, or persisted anywhere. This is worth recording explicitly now: the current confirmation copy already promises something ("we've received your note") that isn't technically true yet, which the pre-launch Feedback review needs to either back with a real destination or soften.

Before launch, the Feedback experience should get its own UX/content review:

- Friendly framing, e.g. **"Found something wrong?"**
- Explain what can be reported: incorrect schedules, incorrect locations, incorrect activity information, broken functionality, general suggestions.
- CTA: **"Send feedback."**
- If a structured form is introduced later, likely categories: Incorrect information · Something isn't working · Suggestion · Other.

**Product principle**: feedback should eventually be treated as part of DropIn's data-quality/QA ecosystem — because DropIn aggregates multiple municipalities across multiple source architectures (Toronto Open Data, ActiveCommunities, PerfectMind), user reports may be the fastest way to surface stale municipal data, source drift, parsing problems, facility/location mismatches, or unexpected schedule changes that an automated refresh wouldn't catch on its own. **Do not build this system yet** — this is a future-phase direction, not a current task.

## 3. Privacy

**Status: written and implemented** (as the "Privacy" `Sheet`, `app/page.tsx`, `privacySheetOpen` — this section's original "not yet written" framing predates that implementation). Must reflect the *actual* production architecture, not generic boilerplate — verified against the real, shipped implementation, not assumed. See §19 for the confirmed information-architecture decision (stays a Sheet, not a dedicated page, despite the "Privacy page" language below predating that decision):

- **A. Location** — why it's requested (distance display and Nearest-first ranking, per Phase 4.2/4.3B/4.4B) and that search itself never requires it.
- **B. Location storage** — the current implementation (`useUserLocation`, `app/page.tsx`) holds coordinates in plain in-memory React state only; confirmed by repeated audit across Phases 4.2–4.4B that precise coordinates are never written to `localStorage`, a cookie, a URL, or Share text, and reset on reload. **This must be re-verified against whatever the final production build actually ships before the Privacy page asserts it** — this document records the current state, not a permanent guarantee independent of future code changes.
- **C. Analytics/cookies** — do not claim "no analytics or cookies" until the production hosting/analytics stack is finalized (none exists yet as of this document). If analytics are added later, the Privacy page must be updated in the same change that adds them, not after. See §11 for the recorded launch-observability decision this bullet anticipates.
- **D. Feedback data** — if a future Feedback implementation collects an email or other identifying information, document what's collected, why, how it's used, and a proportionate retention policy, at the time that implementation ships (not before, since it doesn't exist yet — see §2).
- **E. Hosting/technical logs** — the policy must account for whatever the eventual hosting provider logs at the infrastructure level (e.g., IP/request logs), once a hosting provider is actually chosen.
- **F. Third-party destinations** — DropIn links out to municipal Official Listings and Google Maps/Directions; both have their own separate privacy practices the Privacy page should point to rather than restate.
- **G. Contact** — provide a real way to ask privacy questions (e.g., an email address once one exists for this purpose).

**Design principle**: DropIn collects very little user information by design. The Privacy page should be short, understandable, transparent, and proportional to the product — not a large copied legal template.

## 4. Disclaimer / Terms

**Status: not yet written.** A concise disclaimer is the priority here, not a full Terms of Use document. It should eventually communicate: DropIn is independent; municipal information may change; DropIn cannot guarantee real-time availability; users should verify important details through the Official Listing. **No final legal copy is being written or implemented as part of this document** — this is a placeholder for a future, dedicated pass.

## 5. Local Computer / Production Security & Deployment Readiness

**Architectural principle to preserve**: the public production website must not depend on the developer's local machine or `localhost` in any way. Users interact only with deployed production infrastructure; shutting down the development computer must have zero effect on the public site.

Before deployment, run a dedicated **"Production Security & Privacy Readiness Audit"** — not started, scoped here for later. At minimum it should examine: `.env` files, environment variables, API keys/credentials, git history for accidentally committed secrets, `.gitignore` correctness, `public/` directory exposure, client/server boundaries, API endpoints, CORS where relevant, error messages, debug information, production source maps where relevant, dependency vulnerabilities, security headers, raw source snapshots, canonical datasets, internal documentation, local filesystem paths, build artifacts, logging, geolocation handling, feedback data handling, analytics, hosting architecture, and deployment architecture.

The central question that audit needs to answer: **"What exactly becomes publicly accessible when DropIn is deployed?"** Nothing should be assumed public merely because it lives in the repository — every production data/asset category should be explicitly classified as one of **PUBLIC / SERVER-ONLY / BUILD-TIME ONLY / LOCAL-ONLY / SECRET**.

A few facts already true today, worth carrying into that future audit rather than rediscovering from scratch: `.gitignore` already excludes `.env*`, `/data/raw/`, and `/data/canonical/` (the generated snapshot directories — see `docs/PHASE_3_3_DATA_REFRESH_SNAPSHOT_PIPELINE.md`); no `.env` file currently exists in the repo; `public/` currently contains only the default Next.js starter SVGs, nothing DropIn-specific. None of this constitutes the audit itself — it's just the starting state the audit will need to re-verify, not assume, once real hosting/deployment/analytics decisions are made.

## 6. Data Architecture Principle (Reaffirmed)

Two principles already established in earlier phases, restated here so they survive into the launch-readiness track without needing to be rediscovered:

- **Source truth ≠ presentation** — already documented in `docs/ARCHITECTURE.md` ("Source of Truth Principle") and repeatedly upheld in practice: Phase 4.1A's age-sentinel normalization and every prior activity-name normalization pass modified only *display* logic, never the canonical `ageMin`/`ageMax`/raw source fields underneath. Raw/source-derived information stays auditable even as DropIn normalizes what users see.
- **Local/internal data ≠ public production data** — raw and canonical snapshots, internal refresh tooling, and developer-facing documentation are not to be exposed merely because they're useful during development or the refresh workflow. This is the specific question the Security Audit in §5 will need to verify concretely (what's actually served vs. what merely exists in the repo).

## 7. Launch Readiness Sequence (Provisional)

```
Current Phase 4 work                                    ✅ done
        ↓
Complete remaining Phase 4 product/location QA           ✅ done
        ↓
Launch Readiness                                          ✅ done (this document)
        ↓
About / Info review                                       ✅ done
        ↓
Feedback design                                            ✅ done
        ↓
Privacy + Disclaimer                                       ✅ done
        ↓
Production Security & Privacy Readiness Audit               ✅ done
        ↓
Deployment architecture review                               ✅ done (Phase 5A/5B)
        ↓
Production deployment                                        ✅ FIRST DEPLOYMENT LIVE VERIFIED
        ↓                                                    (2026-08-27, getdropin.vercel.app —
        ↓                                                     see PHASE_5B_PRODUCTION_INFRASTRUCTURE_
        ↓                                                     PREFLIGHT.md §10)
Post-launch monitoring / QA                                  ⬜ next
```

**Status update (2026-08-27): "Production deployment" has had its first real, LIVE VERIFIED instance** — `https://getdropin.vercel.app`, smoke-tested against real production data, full evidence in `PHASE_5B_PRODUCTION_INFRASTRUCTURE_PREFLIGHT.md` §10 and `PHASE_5B_RESPONSE_SIZE_ARCHITECTURE.md` §7. **This is not yet the full public launch** — `getdropin.ca` remains deliberately disconnected (§3/§8 of that preflight, Phase 5A §10's sequencing, unchanged), and §9's mobile/physical-device verification requirement has not yet been performed against this real deployment. The next step in this sequence, per §9's own standing requirement, is mobile/iPhone verification against the now-live production deployment — not started as part of this checkpoint.

**Status update (2026-09-03): this diagram is now superseded by §28**, which records Production Physical-Device Regression QA as complete (CLOSED / PASS) and carries the current, accurate remaining launch sequence — "Post-launch monitoring / QA ⬜ next" above no longer reflects the real next step.

Exact numbering/phase names to be assigned when each step actually begins.

## 8. Map View Remains Deferred

Launch readiness is not a reason to build Map View. The Phase 4.0 decision (`docs/PHASE_4_0_GEOSPATIAL_READINESS_MAP_NECESSITY_AUDIT.md`) stands: **Map View = defer.** Location intelligence continues to center on facility coordinates (Phase 4.1), real geolocation (Phase 4.2), distance display and time-first-then-distance ranking (Phase 4.3A/B), Nearest-first as an explicit opt-in (Phase 4.4/4.4B), and Directions — not a map surface. Map View should only be reconsidered if actual usage data demonstrates a real spatial-browsing need that this location work doesn't already cover.

## 9. Mobile Remains a Release Requirement

Mobile usability stays a production requirement, not a nice-to-have. Before launch, verification is still needed (beyond what's already been checked per-phase) across: narrow-viewport layout, horizontal overflow, touch targets, modal behavior, location permission UX, search, date/activity controls, Nearest first, result cards, Directions, Official Listing, and the About/Feedback/Privacy surfaces once they exist. Physical-device verification should be performed when possible before launch — consistent with this project's established discipline (every phase from 4.2 onward) of never claiming physical-device validation that wasn't actually performed; structural/DOM-constraint verification and physical-device verification remain explicitly distinct and separately reported.

**Status update (2026-08-28): Round 1 real physical-iPhone QA against `https://getdropin.vercel.app` is complete.** Findings were recorded, not treated as evidence the core mobile product was broken — the core Search + Results experience passed; the secondary interaction layer (Results metadata/controls crowding, Activity Detail sheet spacing/dismissal, About/Privacy density, Share content, Directions destination format, redundant title text) needed mobile-specific polish. A targeted **Mobile UX Polish patch has been implemented** in response (`app/_components/Sheet.tsx`, `app/page.tsx`, `lib/dropin/activities.ts`, `docs/PROJECT_WORKFLOW.md` — commit `6120b0f`), covering: a split two-row mobile Results metadata/controls layout, Activity Detail sheet breathing room + a true ~44×44px close target + swipe-down-on-handle dismissal + backdrop-tap dismissal + internal scrolling for long content, tighter About/Privacy mobile rhythm (About's trust-related items grouped under "Data & accuracy," no accordion introduced), a native-Share payload that includes activity name/time/venue/DropIn URL and never precise location, Directions preferring human-readable venue name + address over raw coordinates, and conservative display-only activity-title normalization (e.g. `Public Swim (5:45 p.m.)` → `Public Swim`, only when the trailing time matches the structured session start time; raw source data unchanged). The patch passed build, typecheck, and lint, and passed desktop regression review and resized-browser-viewport verification for the mobile changes.

**IMPORTANT — this patch has NOT yet been physically re-verified on a real iPhone.** Viewport verification in a desktop browser is not a substitute for physical-device verification, per this section's own standing discipline. Current status is precisely:

```
Mobile UX Polish:  IMPLEMENTED + VIEWPORT VERIFIED
                   NOT YET PHYSICALLY VERIFIED
```

**The immediate next checkpoint is Round 2 real-iPhone regression QA** — testing the polish patch on the same real device used for Round 1, against the same production build. Round 2 is not started and must not be described as complete until it has actually been performed.

**Status update (2026-08-28): a Round 2 real-iPhone QA pass against production surfaced two new findings, distinct from the Round 1 Mobile UX Polish patch above** — this entry records only these two findings and does not itself confirm or deny the Round 1 patch's own checklist items (Results metadata/controls, Activity Detail sheet, Directions, Share, title normalization, About/Privacy); that confirmation is not yet recorded here and should not be assumed from this entry.

1. **Searching an informal community name (e.g. "Unionville," a real community within Markham) returned "we couldn't find it."** Investigated end-to-end: this is a genuine data/vocabulary gap, not a bug — the string "Unionville" does not exist anywhere in DropIn's ingested data (no facility name, no address, no district field, across any of the 10 real Markham facilities/addresses checked directly), and `DISTRICTS` (the only neighbourhood-level vocabulary DropIn has) is hardcoded to Toronto's four old boroughs only — no municipality has a general informal-community concept. **This remains open and deliberately deferred** to its own future product-design task (a small curated alias/gazetteer layer, not a geocoding system, is the recommended direction) — not addressed by this entry, not scheduled, not implemented.
2. **A facility search (e.g. "Oakridge") could replace the header's "Near me" text with the full Community Centre name** (e.g. "Oakridge Community Recreation Centre"), which on a real phone could wrap across two lines and distort the header, and was independently judged a semantic error — a single building isn't the broad area/proximity context the header pill exists to communicate. Traced to `locationPillLabel()` in `app/page.tsx` rendering any resolved search location's label unconditionally, regardless of type. **Fixed** (2026-08-28) — a `type: "centre"` result no longer drives the header pill; it continues to correctly filter results exactly as before. Display-layer change only; no change to search filtering, ranking, `sessionMatchesLocation`, user geolocation, or distance calculation. Full mechanics in `docs/SEARCH_ENGINE.md`'s Location Override vs. Persistent "Near You" section and `docs/INFORMATION_ARCHITECTURE.md`'s Location Context section.
3. **The shared mobile Sheet's drag-handle-to-title gap (Round 1's own fix for the previously-flagged handle/title crowding) itself read as too much dead space on a real phone**, in both Activity Detail and About. Traced to `app/_components/Sheet.tsx`'s panel top padding plus the handle zone's own padding stacking to ~56px between the panel's top edge and the title row. **Implemented** (2026-08-28, pending review — not yet committed): mobile-only reduction of both the panel's top padding and the handle zone's own padding (desktop values fully preserved via `md:` overrides), reclaiming roughly 20px while keeping the handle clearly separated from the title — never returning to the pre-Round-1 bare `mb-2` — and keeping a full-width, ~26px-tall touch/gesture zone around the handle. Established as a **shared Sheet-chrome rule**, not a per-sheet patch: because Activity Detail, About, and Privacy all render through the same `Sheet` component, the fix applies to all three identically without touching any of their individual content. Alongside this, Activity Detail's own title (the sheet's primary identity) was bumped from `text-[18px]` to `text-2xl` (mobile only, `md:text-lg` preserves the original desktop size) — an existing Tailwind token at the requested ~24px, not an arbitrary value — with its icon growing in step for visual balance; About and Privacy's titles are untouched, since only Activity Detail's typography hierarchy was flagged. **About's broader information-density problem is explicitly not addressed here** — remains open for a separate future Information Architecture task, same as it was before this entry. Build and typecheck clean; lint has zero new findings (see the pre-existing 10-error baseline already recorded against this same file in the entry above). **Status update: PHYSICALLY VERIFIED on a real iPhone** (2026-08-28), via a Vercel Preview deployment of this branch (`preview/mobile-sheet-refinement`, after the R2 CORS fix in §16 unblocked its data loading) — the reclaimed handle-to-title spacing and the Activity Detail title hierarchy were confirmed comfortable on the real device, satisfying this section's own standing distinction between viewport-verified and physically verified. **One open follow-up noted during this same QA pass, not acted on here:** some long activity titles felt slightly oversized at the new `text-2xl` size, but this may be a symptom of un-normalized titles (raw source text carrying redundant time/facility text) rather than the size itself being wrong — a possible `text-2xl`→`text-lg`-ish (~22px) reconsideration is deliberately deferred until after the separate Activity Title Normalization work (§17) is resolved, so the two variables aren't changed at the same time. **Update (2026-08-28): that normalization work has since shipped and been physically verified on a real iPhone Preview** — see §17. The `text-2xl` size itself is still unchanged, per that same deferral; typography will be reassessed as part of the upcoming Mobile Sheet system task, now that redundant title text is no longer part of what's being judged.
14. **The same real-iPhone Preview QA separately found that the shared Sheet close button (`app/_components/Sheet.tsx`) could render as a very large outlined circle around the ×** — most consistently on About and Privacy, whose sheets autofocus Close on open (Activity Detail focuses Directions instead, via its own `initialFocusRef`). Root cause: `focus-visible:ring-2 ring-sage-text` (present since the original Sheet implementation, well before any Mobile UX Polish work) was applied directly to the same outer element carrying the Round-1-added `h-11 w-11` (44×44) touch target — so the ring, previously subtle around a small ~28px box, now traced the full 44px invisible hit zone whenever Safari treated the sheet's programmatic open-time autofocus as `:focus-visible` (a known cross-browser inconsistency for programmatic `.focus()` calls, which is why it looked normal in some sheet-opens and oversized in others on the same device). **Not introduced by the current Mobile Sheet refinement** — confirmed via `git log -p` that the ring style predates it entirely, and via this branch's own diff that item 3 above never touched this line; Round 1's touch-target enlargement is what inflated the ring's footprint, unnoticed until this round's physical QA. **Fixed** (2026-08-28): the outer 44×44 button is now purely the interactive hit target (sizing, click, keyboard, and screen-reader semantics only, no visible chrome); the actual background/ring/press-scale styling moved to a small inner `<span aria-hidden="true">`, sized to the exact ~28px the visible × already occupied at desktop, wired via Tailwind's `group`/`group-focus-visible` so the ring now traces that small circle instead of the full hit zone — at both breakpoints. The 44×44 touch target, keyboard focus visibility, accessible name (`aria-label="Close"`, still on the actual interactive button), backdrop-tap dismissal, swipe-down dismissal, and desktop sizing are all unchanged; confirmed structurally (all three sheet consumers still resolve to this one shared implementation) and via build/typecheck/lint (zero new findings, same pre-existing baseline). **Status update: PHYSICALLY VERIFIED on a real iPhone** (2026-08-28) — the close button now renders at its intended small size with the giant-ring artifact gone, and the 44×44 interaction target was confirmed intact.

## 10. Future Backlog — Support DropIn (Not Implemented)

**Status: backlog only.** No UI, no dependency, no third-party script, and no Privacy/About copy change has been made for this. Recorded in full here so it can be picked up as a defined item later rather than rediscovered from scratch.

**Goal:** explore an optional way for users who find DropIn useful to financially support the project and its ongoing operating costs.

**Current direction:**
- User-facing concept: **"Support DropIn"** — not "Donate," unless DropIn's legal/organizational status changes in the future (see the constraint below).
- **PayPal** is currently the preferred first candidate for accepting voluntary contributions.
- Contributions must remain completely optional and must never affect access to DropIn.
- DropIn remains free to use, unconditionally.

**Potential future UX** (direction only, not designed or built):
- A small, non-intrusive "Support DropIn" entry point.
- Copy explaining that contributions help support costs such as hosting, data maintenance, and continued development.
- Kept visually secondary to the core recreation-search experience — consistent with this project's existing "Comfortable Before Beautiful" / "Reduce Thinking, Not Information" principles.
- No aggressive fundraising prompts, pop-ups, or dark patterns.

**Before implementation, work through:**
1. Review PayPal integration options and fees.
2. Determine whether an external PayPal payment page/link is preferable to an embedded integration.
3. Review privacy implications, cookies, third-party scripts, and data sharing.
4. Review security implications.
5. Confirm appropriate wording around contributions and project status.
6. Update Privacy / About disclosures if required.
7. Confirm whether any accounting/tax obligations apply before accepting money.

**Hard constraints, carried forward from when this item was first raised:**
- DropIn must never be described as a registered charity or nonprofit, and no copy may imply charitable tax treatment (e.g. tax-receipt language) unless that status is genuinely obtained.
- Any future payment integration is a new trust/security surface — handling money is categorically different from the read-only, no-account architecture this project has maintained throughout Phase 4 and Launch Readiness so far. It must trigger a **fresh privacy/security review before implementation**, not be folded into the existing Production Security & Privacy Readiness Audit (§7) as an afterthought, since that audit was scoped before this idea existed — and it must not be treated as reason to expand that audit's current scope now.

## 11. Launch Observability Strategy

**Status: decision recorded, nothing implemented.** No analytics of any kind is enabled today — confirmed by direct inspection (`app/page.tsx`, zero analytics/tracking code of any kind, unchanged by this entry). This section records the *decision* for when observability is actually built, not a change to current behavior, so the choice doesn't need to be rediscovered from scratch when that phase begins.

DropIn needs lightweight post-launch visibility into whether the site is being used and whether the production experience is healthy — but "needs some visibility eventually" is not the same as "build it now," and the three kinds of visibility below are deliberately kept distinct rather than bundled into one decision or one tool.

### Launch v1 — basic web analytics

**Cloudflare Web Analytics is the preferred initial option**, subject to fresh verification during implementation (per this project's standing discipline: nothing about a third party's actual behavior is trusted from memory — it gets re-checked against that provider's real, current documentation and DropIn's own real, current network behavior at the time it's actually wired up, not assumed now). Its intended role is deliberately narrow: lightweight, aggregate website visibility only — visits/page views, traffic trends, device/browser information, referrers where available, geographic aggregate information where available, and web performance/Core Web Vitals where available. **Not implemented as part of this entry** — recorded as the intended direction only.

### Privacy requirement — binding on whichever future phase implements this

The current Privacy copy (`app/page.tsx`) states that DropIn does not currently use analytics. **That statement is correct today and must not change until analytics is actually enabled** — this document does not authorize an early Privacy-copy update, and none was made. When Cloudflare Web Analytics (or any analytics) is actually enabled in a future implementation phase, that phase must, in order:

1. Verify its actual production data-collection behavior against Cloudflare's current documentation at that time (not this document's description of intent).
2. Re-audit DropIn's actual network/client behavior in the real deployed build — the same "prove it, don't assume it" standard already applied throughout the Security & Deployment Audit.
3. Update the Privacy notice so it accurately describes what is actually collected, in that same change — never a separate, later cleanup step.
4. Not make broader privacy claims than what was actually technically verified.

**Privacy copy must describe actual production behavior, not planned behavior** — this is the operative rule this whole entry exists to protect.

### Product analytics — explicitly NOT launch scope

Detailed behavioral/product analytics (e.g. search performed, zero-result search, normalized activity/category searched, municipality searched, Nearest First enabled, location permission outcome, list/grid view selection, Decision Sheet opened, Directions clicked, Official Listing clicked) are explicitly deferred — **not implemented, not designed in detail, listed here only as illustrative examples of the category being deferred.** See §12 for the formal backlog entry.

Before any future product-analytics implementation, it needs its own dedicated analytics/privacy design phase, covering at minimum: event schema, data minimization, retention, raw search-query handling specifically, location privacy, whether identifiers are necessary at all, and the resulting Privacy-notice changes. **Prefer aggregate/normalized information over storing raw user-entered search queries whenever practical** — a principle to carry into that future design phase, not a design decided here.

### Architecture principle — three separate concerns, not one

DropIn should keep these distinct, and they should not automatically share the same data or tooling merely because they're all loosely "analytics":

1. **Operational observability** — is the site/data pipeline healthy? (Already partly covered by existing tooling — `npm run snapshot:health`'s FRESH/AGING/STALE/UNAVAILABLE classification, GitHub Actions' failure-email notification — both recorded in `docs/PHASE_5A_HOSTING_REFRESH_ARCHITECTURE.md` §5/§6, unrelated to user-facing analytics of any kind.)
2. **Basic web analytics** — is the site being visited and performing well? (§ Launch v1, above — Cloudflare Web Analytics, aggregate only.)
3. **Product analytics** — how are people actually using DropIn? (§12, explicitly deferred, needs its own dedicated design phase before any implementation.)

## 12. Future Backlog — Product Analytics (Not Implemented)

**Status: backlog only.** No event tracking, no schema, no third-party product-analytics tool, and no Privacy-copy change has been made for this. Recorded here, in the same style as §10's "Support DropIn" entry, so it can be picked up as a defined item later rather than rediscovered from scratch.

**Goal:** design a privacy-conscious DropIn product analytics system, only when actual usage justifies it — not merely because it would be interesting to have.

**Do not implement during launch readiness or Phase 5.** Before any implementation, a dedicated analytics/privacy design phase must cover: event schema, data minimization, retention period, raw search-query handling (preferring aggregate/normalized data over storing what a user actually typed, wherever practical), location privacy (DropIn's existing, hard-won browser-only geolocation guarantee must not be quietly weakened by an analytics integration), whether persistent identifiers are necessary at all, and the required Privacy-notice changes — determined at that time, against that implementation's actual real behavior, not written speculatively now.

**Illustrative examples of what this category *might* eventually cover** (not designed, not committed to, not exhaustive): search performed, zero-result search, normalized activity/category searched, municipality searched, Nearest First enabled, location permission outcome, list/grid view selection, Decision Sheet opened, Directions clicked, Official Listing clicked.

## 13. Future Gate — Production Load / Concurrency Verification (Not Implemented)

**Status: gate recorded, not performed.** No load test, no concurrency test, and no tooling for either has been added. This is a required pre-launch checkpoint, not a suggestion — recorded now, while the R2 migration (`docs/PHASE_5B2B_R2_STORAGE_INTEGRATION.md`) is fresh, specifically because it changes DropIn's request-time behavior in a way the existing local-disk-only verification never exercised.

**Why this is a real gate, not a formality:** every verification performed through Phase 5B-2B (Aurora's live write+read, then all 7 municipalities' migration and read-back) was **single-request, sequential** — one `curl` at a time, never concurrent traffic. Production traffic will not look like that. Two things specifically need real-traffic-shaped evidence before public launch, not assumption:

- **R2 read behavior under concurrent load.** `/api/sessions` currently issues up to 7 concurrent R2 `GetObjectCommand` calls per request (`Promise.allSettled` over all municipalities, unchanged by the storage-backend migration). One request doing 7 concurrent reads is already proven working (§ this phase's own verification). *Many simultaneous visitors* each doing that has not been — R2's free-tier request limits (10M Class B operations/month, confirmed during the Phase 5B-1 preflight) are generous, but the actual latency/error behavior under real concurrent load from a real serverless host is a live-traffic question, not a documentation one.
- **Vercel serverless concurrency behavior for this specific request shape.** `/api/sessions` currently returns the full, unfiltered session set on every request (Phase 5A §L's still-open redirect-vs-server-filter decision, deliberately deferred past launch by M1) — a ~24MB uncompressed response per request, confirmed earlier this session. What that looks like under 10, 50, or 100 concurrent requests — memory pressure, cold-start behavior, R2 connection reuse — is unverified.

**Do not implement load testing now.** This section exists to ensure the gate isn't forgotten, not to schedule it. Before public launch, this needs: a real (even if modest) concurrent-request test against a real deployed instance (not local), read against real R2 data (not mocked), with pass/fail criteria decided at that time based on DropIn's actual expected traffic — not a synthetic, arbitrarily large number chosen without evidence, consistent with this project's own "don't build for hypothetical scale" discipline applied everywhere else.

## 14. Aurora ActiveCommunities Data Completeness — CLOSED / LIVE VERIFIED (2026-08-30)

**Status: RESOLVED, live-verified in real production.** Originally discovered during the first real Phase 5B-3 production refresh run (`docs/PHASE_5B3_DAILY_REFRESH_SCHEDULER.md`). Fixed and closed on branch `fix/aurora-centre-partition-fallback` after a dedicated reliability investigation, per the hard constraint below — the Completion Gate itself was never weakened.

**The issue:** Aurora's ActiveCommunities `activities/list` endpoint, for the "group fitness" drop-in keyword, currently reports **30 total records**, but the endpoint caps at **20 results per page** with **no confirmed working pagination mechanism** — verified directly against the live API this phase (real request/response inspection, several plausible pagination parameter shapes tried, none produced a reliable second page; one attempt produced a clearly broken, session-stateful response, reinforcing rather than contradicting the original Phase 3.6B finding that "the server ignores it").

**Why this is not currently breaking anything:** Aurora's existing "Completion Gate" (built in Phase 3.6B specifically for this endpoint's known lack of pagination) is functioning exactly as designed — it detects when a keyword's entire returned page is "Drop In"-prefixed *and* the server's own `total_records` exceeds the page size, and **refuses to treat the fetch as complete** rather than silently under-reporting real data. This is a deliberate, evidence-based safety mechanism working correctly, not a defect.

**Current effect:** Aurora's daily automated refresh fails at the fetch step (before validation is even reached) for as long as this condition persists. Its last-known-good R2 snapshot (`production/canonical/aurora/latest.json`) remains active and is served to users unchanged — it does not go stale to zero, it simply stops getting *fresher* until this is resolved. The other 6 municipalities are unaffected (per-municipality failure isolation, unchanged).

**Hard constraint, carried forward:** **Do not weaken or bypass the Completion Gate** to make Aurora's refresh "succeed" — raising the safety threshold or disabling the check would mean silently shipping incomplete data, exactly what this mechanism exists to prevent. Any real fix must be a genuine, evidence-based pagination mechanism (or another way to retrieve all 30 records reliably), found with the same rigor Phase 3.6A/3.6B originally applied to this source — not a quick patch to make a red workflow turn green.

**Resolution (2026-08-30):** a dedicated Aurora reliability investigation, mirroring Phase 3.6A/B's own methodology, found a genuine complete-retrieval path without pagination and without weakening the gate:

- **Evidence-based centre discovery.** Aurora's `activities/map` endpoint (discovery-only, never treated as the canonical dataset) reports the real, current physical-centre breakdown for a capped keyword — confirmed live that per-centre counts sum exactly to the same `total_records` the capped `activities/list` call itself reports.
- **Sequential per-centre retrieval.** Each discovered centre is re-queried individually through the same `activities/list` endpoint every other keyword already uses. Requests are sequential, not concurrent — live testing found Aurora's session/cookie-scoped backend intermittently returned an empty result for one of several *concurrent* requests on one session; sequential requests against the same live endpoint succeeded consistently.
- **Contradictory-response guard.** Separately, live testing found Aurora's backend can return `total_records > 0` with `items: []` — a genuine contradiction indistinguishable from a real empty result to the original Completion Gate. A genuine `total_records === 0 && items: []` response is never touched. For the contradictory case only, exactly one immediate retry is attempted; if the retry repeats the same contradiction, the fetch **throws and fails closed** — the previous known-good snapshot is preserved, never an incomplete dataset.
- **Every existing safeguard preserved, not replaced:** the original Completion Gate still applies independently to each centre partition; the merged result is only accepted if the summed per-centre totals exactly match the original unfiltered `total_records`; duplicate activity ids across centres are explicitly checked and rejected.

**Live verification:** 10/10 repeated real Aurora fetches after the fix — 27/27 group-fitness activities every time, zero duplicates. 45 raw live keyword calls — 44 clean, 1 correct fail-closed throw (persistent contradiction), zero silent data loss. Real 7-municipality production R2 refresh — 7/7 succeeded, Aurora activated a genuinely fresh 511-session snapshot (not the prior fallback). Confirmed via the actual Daily Production Data Refresh GitHub Actions workflow (**Run #6, branch `fix/aurora-centre-partition-fallback`**): workflow SUCCESS, combined object written and read-back verified (44,393 sessions: Toronto 24,376 / Mississauga 15,935 / Richmond Hill 232 / Vaughan 550 / Markham 646 / Newmarket 2,143 / **Aurora 511**).

## 15. Open Issue — Geographic Vocabulary / Informal Community Search (Non-Blocking)

**Status: unresolved, tracked, non-blocking, deliberately deferred.** Discovered during Round 2 real-iPhone QA (§9) when searching "Unionville" — a real community within Markham — returned "we couldn't find it" instead of a meaningful result. Investigated the same day. **Not implemented, not worked around, not fixed here.**

**The issue:** DropIn's Search understands exactly four geographic tiers: Municipality (14 entries, reference-list), Neighbourhood/District (Toronto's four old boroughs only — North York, Scarborough, Etobicoke, Downtown Toronto — hardcoded in `lib/dropin/districts.ts`), Community Centre (facility name substring match), and Postal Code (FSA pattern). There is no concept anywhere in the pipeline for an informal community name within a municipality. Confirmed this is a genuine data gap, not a matching/indexing bug: the string "Unionville" does not appear anywhere in Markham's canonical session data — not in any of its 10 real facility names, not in any of its real street addresses (all read `"..., Markham, <postal>"`), not in any `district` field (PerfectMind, Markham's source family, supplies no neighbourhood field at all — `district` is a literal empty string on every non-Toronto session). Even Toronto's own source data tops out at the same four-borough granularity — no municipality has finer community-level data available to search against today.

**Why this is non-blocking:** the existing "query didn't resolve to anything recognized" fallback (`docs/SEARCH_ENGINE.md`'s Empty/No-Results Behavior) handles this honestly — it falls back to Discovery scoped to the persistent location, with a line acknowledging the miss, rather than a dead end or a false claim.

**Recommended direction, not committed to:** a small curated alias/gazetteer layer (community name → municipality, resolved via reference-list lookup, the same mechanism `DISTRICTS` already uses) — explicitly **not** a geocoding/boundary-polygon system, which this project's existing discipline (per `docs/PHASE_4_0_GEOSPATIAL_READINESS_MAP_NECESSITY_AUDIT.md`'s Map View deferral reasoning) would consider unjustified infrastructure without stronger evidence of need.

**Not scheduled, not assigned a phase yet.** Needs its own product-design scoping (which communities, which municipalities, how much coverage) before implementation — deliberately kept separate from the header-display fix in §9, which addressed a different, unrelated finding from the same QA round.

## 16. Open Issue — No Error State for a Failed Session Fetch (Non-Blocking)

**Status: unresolved, tracked, non-blocking.** Discovered incidentally (2026-08-28) while investigating a real-iPhone Preview-deployment data-loading failure — the underlying cause of that specific failure was a missing R2 CORS origin for the Preview URL (§9's Mobile Sheet Preview branch), **since resolved**: the project owner added the exact Preview origin (`https://getdropin-git-preview-mobile-1f6980-qingwen0722-3947s-projects.vercel.app`) to R2's `AllowedOrigins`, append-only, nothing else in the policy changed. Verified directly afterward — both a CORS preflight (`OPTIONS`) and an actual `GET` against a fresh presigned R2 URL, sent with the Preview origin, now return the correct `Access-Control-Allow-Origin` header (matching the same successful pattern already confirmed for the production origin, which was re-verified unaffected throughout). This §16 finding itself is a distinct, separate gap the investigation surfaced along the way, not the CORS issue. **Not fixed here, not worked around.**

**The issue:** the client-side session fetch (`app/page.tsx`, the `useEffect` that calls `fetch("/api/sessions")`) has no `.catch()` anywhere in its promise chain:
```js
fetch("/api/sessions")
  .then((res) => res.json())
  .then((data) => { ...; setLoading(false); });
```
If that fetch ever rejects — a CORS block (as happened here), a network failure, a transient R2/Vercel outage — the `.then` chain never runs, `setLoading(false)` is never called, and `sessions` never leaves its empty initial array. The user-visible result: skeleton result cards stay up indefinitely, any activity count reads `0`, and there is no error message and no retry affordance. This is a real UX gap independent of what actually triggers the failure — any future fetch failure, for any reason, would reproduce the same stuck-skeleton symptom.

**Why this is non-blocking today:** the one real occurrence found (the Preview CORS gap) has a known, narrow root cause with its own fix path (§9); this finding is about the *absence of a graceful failure state* in general, not a currently-active production bug — production's own fetch path is working (confirmed live, §9).

**Recommended direction, not committed to:** add a `.catch()` that sets a distinct error state and surfaces a real, honest message (consistent with this project's "never imply certainty the data can't support" principle) plus a manual retry action, rather than leaving the skeleton as an implicit "still working" signal that can't distinguish "loading" from "failed."

**Not scheduled, not assigned a phase yet.** Deliberately not fixed as part of the CORS investigation that surfaced it — recorded here so it isn't lost, per this document's own "don't rediscover from scratch" discipline (same treatment as §14/§15).

## 17. Activity Title Normalization + Activity Icon Consistency

**Title normalization — RESOLVED, PHYSICALLY VERIFIED (2026-08-28).** Round 2 QA found redundant title text (e.g. `"Group Fitness - Restorative Yoga (4:45 p.m.) - Magna"` next to a card already showing the same time and facility) concentrated entirely in Newmarket (60 of 161 distinct titles; 0 in every other municipality). Fixed with a narrowly-scoped, evidence-gated step in the shared `displayActivityName()` pipeline (`lib/dropin/activities.ts`) that strips a trailing facility abbreviation (`Magna`/`RTRC`/`Gorman`) only when it exactly matches the session's real `centre` — which also unblocks the pre-existing embedded-time stripper for the same titles. Regression-verified against the full real dataset (all 7 municipalities, 749 distinct titles): exactly the 60 Newmarket titles changed, zero elsewhere. Richmond Hill's separate `"- Adult"`/`"- Teen"` case was audited and deliberately left unchanged — real contradicting data exists elsewhere in the same qualifier tables (e.g. Markham's `"Pickleball: Adults"` includes real `ageMin=10` sessions), so bypassing the age-safety gate wasn't proven safe. **Physically verified on a real iPhone Preview deployment** — materially cleaner, confirmed by the project owner.

**Minor follow-up, not a blocker:** physical QA also surfaced a small punctuation inconsistency — some titles read `"Pickleball-Open Play"` where `"Pickleball - Open Play"` (spaced hyphen) would be more natural. **Deliberately not patched now** — recorded as a normalization-polish follow-up, not reopened immediately after the verified fix above.

**Activity Icon Consistency — RESOLVED via product decision, not expanded matching (2026-08-28).** Investigation found the Activity Detail title's conditional sport/activity icon (`getShortcutForActivity` → `ACTIVITY_ICONS`) only ever resolved for a small, Toronto-centric slice of real titles (12/230 for Toronto; ≤1/196 for every other municipality — several municipalities at 0), so most Detail sheets showed no icon regardless. Rather than grow that lookup into a municipality-wide taxonomy, the product decision is **"icons support discovery, not identification"**:
- Homepage quick-start discovery chips — icons intentionally **kept** (`app/page.tsx`, `SHORTCUTS.map`/`ACTIVITY_ICONS[chip]`, untouched).
- Results cards — intentionally remain **text-first**, no activity icons introduced.
- Activity Detail Sheet title — the conditional icon is **removed**; the title is now consistently text-only. Structural header is now `[Activity title] [Close]` rather than conditionally `[Icon] [Activity title] [Close]`, with no reserved empty icon space — the title uses the full available width.

Confirmed isolated before implementing: `getShortcutForActivity`'s only other caller (`activityDisplayLabel`, a text label for multi-activity search results) and `ACTIVITY_ICONS`'s homepage-chip usage are both untouched — removing the Detail-title icon touches nothing else. Activity Detail title typography (`text-2xl`/`md:text-lg`) is unchanged, per §9 item 3's own deferral — it will be reassessed together with the Mobile Sheet system task.
## 18. Infrastructure Maintenance Backlog (Non-Blocking)

**Status: recorded only, not fixed.** Two small findings surfaced incidentally while closing §14's Aurora gate (Daily Production Data Refresh, Run #6) — neither is an Aurora reliability issue, both deliberately left as-is here.

- **Job-summary parsing.** The GitHub Actions run summary displayed "Could not parse refresh output as JSON" despite the underlying refresh succeeding — a reporting/formatting gap in `scripts/refresh/format-summary.ts`'s handling of the real CLI output shape, not a data-integrity or Completion Gate issue. The workflow's actual pass/fail result and the real per-municipality data were unaffected.
- **Node.js 20 deprecation.** The workflow runner logs a deprecation notice (Node 20 forced onto Node 24 runners) and the AWS SDK v3 warns it will require Node ≥22 after early January 2027. Not a current failure, not a launch blocker — a future runtime-upgrade housekeeping item for `daily-refresh.yml` and `actions/setup-node`.

## 19. About / Privacy Information Architecture — RESOLVED (2026-08-31)

**Status: decided and implemented for v1**, via the Mobile Sheet System's Prompt 4A (audit) and Prompt 4B (refinement) on `preview/mobile-sheet-refinement`.

**Decision (Prompt 4A):** both About and Privacy remain shared-Sheet consumers — `desktopVariant="modal"`, bottom sheet on mobile / centered modal on desktop, same as Activity Detail's overlay mechanism but without its contextual/action-oriented content. Neither becomes a dedicated route in v1. This was reached by inspection, not assumption: neither surface currently has deep-link/shareable-URL support or any router usage at all (the app is a single `"use client"` page with no routes beyond `/api/sessions` and `/design/*`), and Privacy's actual content — 6 short sections, each a heading plus 1–2 sentences — turned out to be the same order of magnitude as About's, not meaningfully longer despite the original assumption that it "may contain more content than a typical lightweight sheet." A dedicated route would have introduced real architectural cost (new route, its own navigation/back semantics, risk of losing search/filter context) that the actual content doesn't justify, against this project's own "preserve the user's search/results context whenever practical" and "avoid government-portal complexity" principles.

**Explicitly deferred, not forgotten:** Prompt 4A's own recommendation to sync each Sheet's open/closed state to a URL query parameter (`?info=about` / `?info=privacy`) for shareability and correct browser Back behavior was **not implemented in Prompt 4B**, per explicit instruction — judged unnecessary launch-stabilization-phase complexity for a nice-to-have. The underlying gap this leaves (a phone's native back-gesture navigates away from the app rather than closing the sheet, since no history entry exists for sheet state) is the same pre-existing gap that predates this audit and remains open, not a regression introduced here.

**Prompt 4B implementation status:** reviewed against Prompt 4B's explicit hierarchy/spacing/typography requirements (clear heading-vs-body distinction, comfortable mobile reading rhythm, secondary/muted meta text, an actionable-looking Privacy entry point, no excessive spacing, no dense legal-document styling) — **the existing markup already satisfied every one of them**, being the product of an earlier, dedicated Mobile UX Polish pass (§9/§16/§17) that had already reworked both sheets' internal spacing and grouping. No JSX/typography/spacing changes were made to either sheet as a result; inventing changes against already-correct, already-reasoned code would have contradicted this project's own "don't redesign what isn't broken" discipline. `app/_components/Sheet.tsx` was not touched, consistent with Prompt 3's established shared-Sheet spacing baseline.

**Verification:** live-tested in-browser (not inferred from code) — About opens correctly with clearly distinguishable sage-colored headings over muted body text; the About → Privacy transition (tapping "See what DropIn does and doesn't collect") replaces About with Privacy correctly, via a brief (~200ms, matching the Sheet's existing `EXIT_DURATION_MS`) crossfade during which both are momentarily mounted — a standard, self-resolving transition artifact, not a defect; closing Privacy returns to the exact prior search/results state with no reset; desktop centered-modal presentation confirmed for both. As with Prompt 3, this sandbox's browser tooling cannot render a genuine sub-768px mobile viewport (fixed at a desktop resolution regardless of window-resize calls), so true mobile-breakpoint pixel verification for this specific pass still requires physical-iPhone confirmation — the same limitation already recorded for Prompt 3.

## 20. Directions — Mobile Map-Provider Chooser — CLOSED / PHYSICALLY VERIFIED (2026-08-31)

**Status: implemented on `preview/mobile-sheet-refinement` and physically verified on a real iPhone.** Round 2 physical-device QA found the Directions CTA (`directionsUrl()`, `app/page.tsx`) always assumed Google Maps, and some destinations still risked resolving as an unlabeled coordinate rather than a recognizable venue. A dedicated investigation (POST-QA Polish 01) audited the real, live production destination data across all 7 municipalities before implementing anything: `centre` is present on 100% of the 44,357 real sessions checked; Toronto/Vaughan/Markham/Newmarket have address data on effectively 100% of their sessions; Mississauga/Richmond Hill/Aurora (the ActiveCommunities family, Newmarket excepted) have **0%** address/postal data, relying on `centre` + coordinates only; **1,642 real sessions (3.7%), all in those same three municipalities, have neither address nor coordinates** — for those, every provider necessarily falls back to a municipality-only query, a genuine data-completeness limit this patch does not attempt to fix (no geocoding service was added, per explicit instruction).

**Physical-iPhone verification (2026-08-31):** real-device pass on a live production session, **PASS**.
- **Mobile provider chooser:** PASS — opened correctly on a real iPhone.
- **Apple Maps:** PASS — a Toronto venue with full destination data (centre + address) opened as a recognizable named destination, not a raw coordinate pin.
- **Google Maps:** PASS — the same Toronto venue resolved to the correct Google Maps place entity.
- **Missing-address fallback, tested against a real §20 edge case:** PASS. Test session: **"Fun Swim"** at DropIn centre value **"Lions Club of CV Main + Leisure Pool"**, Mississauga — a real session with no address and no coordinates in the canonical data (per this section's own audit above). The Activity Detail sheet correctly showed no street address (honest about what data it has, no invented value). Despite that, Google Maps resolved the centre+municipality query correctly to **"Lions Club of Credit Valley Outdoor Pool"** — the real venue, not a municipality-wide or mismatched result.
- **Conclusion drawn from this result:** Directions should **not** be hidden merely because address or coordinates are missing — centre/venue name + municipality is a genuinely useful provider-level place-search fallback, confirmed working on at least one real case from the exact affected population. This is one verified data point, not proof every one of the 1,642 affected sessions resolves this well — the underlying address/coordinate data-quality gap itself remains open and unfixed, per this section's original scope.

**What shipped:** the existing Google-only `directionsUrl()` was generalized into a shared `directionsQuery()` (same priority chain as before: centre+address+municipality → centre+municipality → municipality-only) plus two provider-specific URL builders, `googleMapsUrl()` and `appleMapsUrl()`, both built from the identical underlying destination data — no divergent logic per provider. On mobile viewports only (a responsive-layout check against the same `768px` breakpoint `Sheet.tsx` already uses, not user-agent/platform sniffing — deliberately not implemented, per the investigation's own finding that installed-app detection isn't reliably available to a web page), tapping Directions opens a small secondary Sheet ("Open directions with" — Apple Maps, Google Maps) instead of navigating immediately; desktop keeps its exact prior one-click Google Maps behavior, untouched. The chooser reuses `Sheet.tsx` exactly as-is — no changes to the shared component. It's the first place in this app where two Sheets are genuinely open at once (Activity Detail stays open underneath, rather than being replaced the way Privacy replaces About) — Activity Detail's own `onClose` gained one small guard so Escape closes only the topmost sheet instead of both simultaneously; no other Sheet-stacking behavior needed changing.

**Remaining deferred, still open after physical verification — not blockers to closing this section:** Waze (weaker venue-name matching per the investigation, a third provider to physically verify for marginal launch benefit — remains deferred, not rejected outright); provider-preference persistence (a simple always-ask chooser remains judged sufficient for launch — no evidence yet that users need this remembered); the Mississauga/Richmond Hill/Aurora address-data gap itself (source/ingestion-level, not a Directions-feature problem — the physical test above shows the fallback often works well, but the underlying gap is unfixed and un-geocoded); and 42 real Aurora sessions with `centre: "Virtual"`, a non-physical activity for which "Directions" is semantically meaningless regardless of destination-data quality — a separate, unresolved product/data decision (hide or relabel Directions for virtual sessions), kept in the backlog, not solved incidentally alongside the map-chooser work.

**Privacy copy updated in the same pass** (§3, "Other websites"): the Google-Maps-specific sentence now reads "...and to your chosen map app for directions" — the minimal provider-neutral wording change this feature requires, made in the same change that ships the behavior, per this document's own established discipline (§11/§3.C's analytics precedent).

**Superseded by §29 (2026-09-03):** the provider chooser recorded in this section was removed following physical-device QA (§28) — Directions now opens Google Maps directly, on every viewport, with no chooser. The venue-aware destination logic (`directionsQuery()`/`googleMapsUrl()`) and this section's underlying data-coverage findings are otherwise unaffected and still accurate. The Privacy wording change recorded above ("your chosen map app") was itself superseded by §29's Google-Maps-specific wording, since the chooser it described no longer exists.

## 21. Share — Payload Polish (2026-09-02)

**Native share mechanism:** physically verified on a real iPhone in an earlier Mobile QA pass — the native iOS share sheet itself works correctly. What that pass flagged as still weak was the *content*: `navigator.share`'s `title`/`text`/`url` were all carrying the same duplicated blob, and the `url` pointed at the municipality's own official listing (or nothing) rather than back to DropIn.

**What shipped (POST-QA Polish 02B):** `handleShare()` (`app/page.tsx`) now gives `title`/`text`/`url` distinct roles — `title` is the activity name alone, `text` is time + venue (address appended only when the session actually has one — reusing the same "don't invent it, use it when present" discipline already established for Directions, §20), `url` is the existing `PUBLIC_SITE_URL` constant, unchanged. The clipboard fallback (desktop/unsupported browsers) is a genuinely separate string from the `navigator.share` text, since a pasted clipboard string gets no separate title treatment — it includes the activity name inline so it reads as a complete, standalone message. Verified live (stubbed `navigator.share`/`navigator.clipboard.writeText`, not just read from code): both an address-present Toronto session and an address-absent Mississauga session produced correct output in each path, and no user location, coordinates, internal id, `sourceScheduleId`, search state, or infrastructure detail appears anywhere in either payload — confirmed by construction and by direct inspection of the captured values.

**Per-activity deep linking — explicitly deferred, not built here.** A dedicated investigation (POST-QA Polish 02) compared real production data across two actual consecutive daily refreshes (2026-08-30 → 2026-08-31) and found session `id` is genuinely unstable for a meaningful share-link-breaking fraction of sessions, concentrated specifically in Toronto (2.31% of matched real sessions) and Mississauga (0.63%) — together **~91% of all DropIn sessions**. `sourceScheduleId` (the existing code's own prior, untested hope for a more stable identifier) turned out to be **only a real improvement for 5 of 7 municipalities** — for Toronto and Mississauga specifically, it churns at the *same* rate as `id`, because both are derived from the same regenerable upstream source identifier in those two adapters. `sourceScheduleId` is also not currently exposed to the client at all (`scripts/refresh/build-combined.ts`'s `TrimmedSession` strips it, a deliberate Phase 5B response-size decision). Building a real stable deep link would require resolving this identity gap first — not a Share-feature change, a data-architecture one. Tracked here as a **post-launch product/architecture item**, not scheduled to a phase yet: the shared URL continues to point at the DropIn homepage (`PUBLIC_SITE_URL`) rather than any specific activity, exactly as before this pass.

## 22. PerfectMind Forward Horizon + Summary-Parser Fix — RESOLVED (2026-09-02)

**Vaughan pagination-density incident:** a real scheduled production refresh failed with Vaughan / Fitness Centre unable to reach a genuine completion signal within the existing 40-page safety cap. A dedicated investigation found PerfectMind's date-cursor pagination itself was working correctly — the cause was a hardcoded, effectively-unbounded far-future request bound (`~2027-06-30`, nearly 10 months out) combined with a real, recent, dramatic density surge in that specific category: the same category needed only 8 pages/257 sessions in the last known-good snapshot (2026-08-30) but did not reach a genuine stop even at 100 pages three days later. A follow-up investigation compared 30/60/90-day candidate horizons against real live pagination measurements across Vaughan, Markham, and Newmarket before choosing one.

**Resolution:** the PerfectMind source-family transport layer (`lib/dropin/sources/perfectmind/client.ts`, shared by all three municipalities) now requests a **rolling 30-day forward horizon** from each refresh's own "today," computed once per category pull, instead of a fixed calendar date. This is a deliberate product-scope decision — DropIn's actual user-facing commitment is a 7-day quick-navigation window; 30 days gives generous operational headroom without chasing unbounded far-future municipal programming — **not a workaround for pagination**, and it does not change what the Completion Gate accepts as complete. `MAX_PAGES_PER_CATEGORY` remains unchanged at **40**; live-measured real need at the new 30-day horizon (Vaughan / Fitness Centre: 23 pages) preserves real headroom under that cap, at roughly the same margin the original 22-page measurement had when 40 was first chosen. The Completion Gate itself (`perfectmind/index.ts`) was not touched — it still throws on any incomplete category, and a failed municipality still preserves its previous known-good snapshot untouched, exactly as before.

**Live-verified** (real production code, not simulated): Vaughan (Sports 19 pages, Fitness Centre 23 pages, Swimming & Aquafitness 2 pages — all complete), Markham (all 10 categories, 2–3 pages each), and Newmarket (all 6 categories, 2–5 pages each) all reach genuine completion well under the cap, with every municipality's returned data correctly spanning exactly today through 30 days out — no truncation at the page-cap boundary. A real single-municipality production refresh (Vaughan) confirmed the fix end-to-end: `fetchStatus: "success"`, a fresh 1,610-session canonical snapshot activated, replacing the stale 2026-08-30 last-known-good snapshot.

**Separately, the GitHub Actions summary-parser issue** (recorded non-blocking in §18) is fixed: the workflow now invokes `npx tsx scripts/refresh/index.ts --all --json` directly instead of `npm run refresh:data -- --all --json`, which was confirmed to prepend npm's own command banner to stdout ahead of the real JSON payload, breaking `format-summary.ts`'s `JSON.parse`. `format-summary.ts` itself was not changed — no loosened parsing, no tolerance for non-JSON text. Verified live: the new invocation produces `refresh-output.json` beginning directly with `{`, `JSON.parse` succeeds, and the renderer produces the correct markdown summary; a synthetic mixed-result payload confirmed municipality failures still render accurately ("FAILED — previous snapshot kept" plus the real failure reason), not hidden by the fix.

## 23. Newmarket One-Time Baseline Transition (2026-09-02)

**Status: complete.** The real scheduled Daily Refresh that first exercised §22's 30-day horizon in full production (Run #12) found Newmarket's fresh 484-session fetch correctly rejected by the `>50%` count-collapse gate (`lib/dropin/snapshot/validate.ts`) against its old, still-active, unbounded-horizon baseline (2,064 sessions — genuinely correct behavior for an intentional ~4.3× step-change the gate had no way to know was expected). A follow-up investigation proved this could not self-resolve: `checkCountCollapse`'s comparison value is read fresh from `canonical/newmarket/latest.json` every run, and a rejected refresh never writes to that path (only to the `raw/*` debug layer) — so every future 30-day fetch would keep comparing against the same stale 2,064 baseline indefinitely without an explicit, one-time intervention. (Markham crossed the same architectural transition in the same run, but only by a narrow ~2.9% margin — 282 vs. a 274 rejection floor — not by any structural safety margin; Vaughan never encountered the gate at all, since its own pre-fix baseline was itself already small/stale from the same pagination problem this whole fix addresses.)

**Resolution:** a real Newmarket-only fetch was run through the actual production functions (`fetchAndNormalizeMunicipality`, facility-location enrichment, `validateCanonicalSessions`) — all 6 expected categories present, every category reaching genuine pagination completion at 2–5 pages (none near the 40-page cap), 484 sessions, date range exactly today through 30 days out, matching the already-verified ~484–491 candidate range within 1.4%. That exact reviewed snapshot was then promoted to `canonical/newmarket/latest.json` (and `canonical/newmarket/previous.json` rotated per the existing 2-slot retention) using the project's own `SnapshotStorage.writeAtomic` primitive directly — the same mechanism `refreshOneSource` itself uses for a normal activation — deliberately without invoking `checkCountCollapse`, since that one-time human review *is* the "stronger evidence this is real" the gate's own message asks for. No file in the codebase was changed to do this: `checkCountCollapse`, `validateCanonicalSessions`, and `refreshOneSource` are all byte-identical to before, and no Newmarket-specific exception exists anywhere.

**Verified immediately after:** promotion confirmed via direct R2 read-back (484 sessions, correct metadata); `canonical/_combined/latest.json` rebuilt successfully via the real `build-combined.ts` script, Newmarket correctly showing 484; Vaughan (1,607), Markham (282), and Aurora (485, explicitly untouched throughout) all confirmed unchanged. **Critically**, a second, completely normal Newmarket-only refresh was then run through the standard, fully-gated pipeline (`scripts/refresh/index.ts --municipality=newmarket`, no bypass, no special flag) — it succeeded cleanly (`fetchStatus: "success"`, `activated: true`, 484 sessions, no `failureReason`), proving the system now compares 30-day-horizon data against a 30-day-horizon baseline and passes on its own. Newmarket is back on the fully autonomous daily-refresh path with no standing exception of any kind.

## 24. Production Daily Refresh / Canonical Snapshot / Failure-Isolation Infrastructure — CLOSED / END-TO-END PRODUCTION VERIFIED (2026-09-02)

**Status: CLOSED.** The real, scheduled Daily Production Data Refresh (**Run #13**, GitHub Actions) completed with workflow status **SUCCESS** and **7/7 municipalities refreshed successfully** in one production run — the first clean 7/7 run since §22's PerfectMind horizon fix and §23's Newmarket baseline transition both landed: Toronto 24,376 · Mississauga 16,836 · Richmond Hill 216 · Aurora 485 · Vaughan 1,604 · Markham 278 · Newmarket 484. Combined canonical object: 44,279 sessions, 26.34 MB, **read-back verification PASS**.

This run is the closing, end-to-end confirmation of everything §14 (Aurora), §20 (Directions map chooser's own data-quality audit), §22 (PerfectMind horizon), and §23 (Newmarket baseline transition) each verified individually:

1. **All 7 municipalities** refreshed successfully in a single real production run — not a partial or simulated result.
2. **Vaughan** passed normally under the new rolling 30-day PerfectMind horizon (§22) — no page-cap failure, no special handling.
3. **Newmarket** passed normally after its one-time, manually-verified baseline migration (§23) — the standard `>50%` drop-protection gate (`lib/dropin/snapshot/validate.ts`) remains completely unmodified; Run #13 is live proof the migration held and needs no further intervention.
4. **Aurora** recovered normally on its own — the transient upstream ActiveCommunities contradiction observed in Run #12 did not recur, and no code intervention was made or needed (per the earlier investigation's own recommendation: transient, retry later).
5. **Combined canonical generation and read-back verification passed** — the same `build-combined.ts` mechanism that already carried §14's and §23's own verifications, confirmed again end-to-end in a fully autonomous scheduled run.
6. **Failure isolation is proven, not just designed-for**: the preceding runs (Run #12's Newmarket/Aurora failures, the earlier Aurora/Vaughan incidents) each demonstrably preserved their previous known-good snapshot rather than activating incomplete data — Run #13's clean 7/7 result is the system recovering from exactly that state on its own once the underlying conditions were resolved, not a fresh, never-tested path.

**Non-blocking follow-ups, explicitly not closed by this milestone, tracked separately:**
- **Node.js 20 deprecation** (§18) — future Node ≥22 runtime-upgrade housekeeping for `daily-refresh.yml`/`actions/setup-node`. Not a current failure.
- **Combined payload size (~26 MB) / production response-size and load-concurrency verification** (§13) — the required pre-launch concurrent-traffic gate remains unperformed, unrelated to this milestone's correctness; still needs real concurrent-request evidence before public launch, per §13's own standing requirement.

## 25. Production Security & Privacy Readiness Audit — CLOSED / PRE-LAUNCH VERIFIED (2026-09-02)

**Status: CLOSED.** The dedicated audit §5 called for (and §7's sequence diagram had prematurely marked "done" without one ever having actually been performed) has now genuinely been run against the real, current production architecture. **No P0 findings.** Exactly one P1 finding, now resolved.

**The P1 finding:** Privacy copy was silent on standard hosting-provider request logging (IP address, request path, browser/device information, timestamps) — true in production (Vercel, like any host, logs this by default) but undisclosed, a gap §3.E had already anticipated needing revisiting "once a hosting provider is actually chosen." **Corrected in this pass**: a new "Hosting & technical logs" section added to the Privacy sheet (`app/page.tsx`), provider-neutral wording (no specific host named, so the claim doesn't go stale if hosting ever changes), explicitly stating this information is never used for advertising or behavioral tracking. No other Privacy claim was touched.

**Verified in the same pass, all PASS, no code changes required for any of these:**
- **R2 read/write credential separation** — confirmed both in code (the app's read path is architecturally incapable of constructing a write-capable client) and against the actual deployed Vercel configuration (documented: write credentials deliberately not present in Vercel).
- **No production public write path exists** — the only API route (`/api/sessions`) takes no request input at all; the canonical snapshot path it reads is a fixed constant, never user-influenced.
- **No secrets exposed to the client** — zero `NEXT_PUBLIC_*` variables anywhere in the codebase; no hardcoded secrets in tracked files.
- **Geolocation remains explicit-action-only and local to the device** — `requestLocation()` fires from exactly one real `onClick`, never automatically; the user's own coordinates are used only in one local distance calculation, never in a URL, Share payload, or any outbound request.
- **No analytics/tracking is currently active** — confirmed by direct code inspection, unchanged.
- **Feedback remains mailto-only** — no backend endpoint exists; DropIn's own code never receives or stores the message.
- **R2 CORS for `getdropin.ca`** — already correctly configured ahead of time (a correction to this audit's own prior investigation, which had mistakenly flagged this as still outstanding).
- **`npm audit`'s 4 high-severity findings** (sharp, postcss, nanoid, next) — all confirmed to have no real trigger path in this application's actual runtime (no `next/image` usage; postcss only ever processes DropIn's own trusted stylesheet at build time).

**Remaining P2 items, explicitly not addressed here, not blockers:** `/design/*` mockup pages being publicly reachable (a scope/cleanup question, not a security risk — confirmed no sensitive content); routine dependency updates for the 4 `npm audit` findings above; no custom security headers (CSP, etc.) configured yet; Node ≥22 upgrade (already tracked in §18).

## 26. Production Load / Concurrency Verification — CLOSED / PRODUCTION VERIFIED (2026-09-03)

**Status: CLOSED.** The required pre-launch gate §13 called for (real concurrent-request evidence against the live deployment, never performed until now) has been run against `https://getdropin.vercel.app`, read-only, real production traffic on the actual session-data path — not `/` alone, not simulated.

**Architecture finding:** `/api/sessions` never holds or proxies the payload itself — its `GET()` handler takes no request input, generates a fresh presigned R2 URL via a local, network-free cryptographic signing operation, and returns a `307` with an empty body (confirmed live: `content-length: 0`, `x-vercel-cache: MISS`, never cached). The actual ~31.7 MB object (33,276,633 bytes measured live — the previously-quoted "~26 MB" figure has grown with real data since it was last measured, updated here with a real number) is served directly by Cloudflare R2/CDN (`server: cloudflare`) straight to the browser, entirely bypassing Vercel's function body/bandwidth for the heavy part. Concurrent visitors therefore cause N independent, parallel direct-to-R2 downloads, not N repeated server-side reads through Vercel. One real architectural caveat: because each presigned URL is uniquely signed per request, the payload is not browser-cacheable across separate page loads — every fresh visit is a full re-download, addressed further below.

**Measurements** (all real, live, production; read-only; no refresh triggered, no snapshot touched, no configuration changed):
- **Baseline** (5 sequential requests): 5/5 succeeded, `200` every time, identical 33,276,633-byte payload every time, `time_total` 1.15–2.99s (median ≈1.50s).
- **Stage A (5 concurrent):** 5/5 succeeded, 0 failures, TTFB median 0.45s / p95 0.50s.
- **Stage B (10 concurrent):** 10/10 succeeded, 0 failures, TTFB median 0.50s / p95 0.76s.
- **Stage C (20 concurrent):** 20/20 succeeded, 0 failures, TTFB median 0.62s / p95 0.76s.
- **Stage D (30 concurrent, optional, run since A–C were clearly healthy):** 30/30 succeeded, 0 failures, 0 timeouts, TTFB median 0.46s / p95 0.79s.
- **Every single request across all 4 stages** (65 total real requests) returned `200` with the exact identical 33,276,633-byte payload — zero corruption, zero truncation, zero throttling/rate-limiting signal observed.
- Total transfer time (`time_total`) rose with concurrency (median 2.9s→6.0s→12.9s→17.5s across stages) — attributed to this test's own single-machine downlink bandwidth being shared across simultaneous ~32MB downloads, not server-side degradation: TTFB (the metric that actually reflects Vercel/R2 responsiveness, independent of the tester's own bandwidth) stayed flat and healthy throughout, with no degradation trend from 5 to 30 concurrent clients.

**~31.7 MB combined payload classification: D — effectively mitigated by the current delivery architecture, for this launch's realistic traffic shape.** Server scalability: not a concern — Vercel's function work per request is trivial (one presigned-URL signature), and R2/Cloudflare's CDN, not Vercel, absorbs the actual transfer load, confirmed healthy at 30 concurrent real downloads. Bandwidth: real cost exists (R2/Cloudflare egress, generous on R2's own terms) but was not the constraint in this test. User-perceived performance: real and worth continued attention — every visit is a genuine ~1.1–3s (this test's local-network conditions) full download with no progress indication and no cross-visit caching, since presigned URLs are unique per request. Mobile-network implications: the same full-payload-every-visit characteristic means a slow mobile connection experiences a proportionally longer blank/skeleton period — a real UX cost, not a reliability failure. Vercel implications: none of concern, confirmed by the architecture trace and the clean 30-concurrent result. R2 implications: none of concern within this test's scope.

**Failure/degradation review (analysis only, no failures intentionally induced):** many users requesting the identical snapshot simultaneously is exactly what Stage D tested directly, live — no degradation found. A temporarily unavailable `/api/sessions` or failed R2 fetch would surface through the already-documented, still-open §16 finding (`app/page.tsx`'s session-fetch has no `.catch()`) — the skeleton would stay up indefinitely with no error/retry affordance; this is a pre-existing, already-tracked, non-blocking gap, not a new finding from this test. A slow client connection simply experiences a longer full-payload download with no adaptive/progressive loading strategy today — a real, honest UX characteristic of the current architecture, not a defect this gate is scoped to fix.

**No P0 or P1 findings from this gate.** Remaining P2 follow-ups: no cross-visit browser caching of the session payload (an inherent consequence of unique-per-request presigned URLs); no download-progress indication during the initial load; §16's missing `.catch()` (already tracked); revisiting payload size/format if it continues to grow materially post-launch.

## 27. Pre-Launch Backlog Reconciliation — Feedback/Privacy CTA + Geographic Search (2026-09-03)

**Feedback / Privacy CTA consistency — CLOSED.** Not a previously-tracked item under this name anywhere in this document; investigated fresh. The About sheet's Feedback CTA (`app/page.tsx`) is a standalone `<a href="mailto:...">` styled `text-sm font-medium text-sage-text underline underline-offset-2`; the Privacy CTA is a `<button>` (correctly, since it opens the Privacy Sheet in-app rather than navigating away — the two are intentionally different element types, not a bug) that was missing `font-medium`, rendering visibly thinner than the Feedback link. Confirmed live in-browser (zoomed screenshot), not just from class names. **Fixed**: added `font-medium` to the Privacy button's className — the one-class change this finding needed, no wording, layout, spacing, navigation, or Sheet-behavior change. Typecheck, build, and lint all clean (lint: existing 21-problem baseline, zero new findings).

**Geographic Search / Unionville — confirmed DEFERRED P2, unchanged.** Re-tested live against real, current production data (44,279 sessions) using the actual `parseQuery`/`sessionMatchesLocation` functions: "Markham," "Vaughan," "North York," and "Scarborough" all resolve correctly today (278 / 1,604 / 5,405 / 4,324 matching sessions respectively) — general municipality/district search works. "Unionville" still resolves to nothing (`location: undefined`) — confirmed by direct data check that the string doesn't exist anywhere in current centre names, addresses, or district fields (Markham's 9 real centres: Aaniin, Armadale, Centennial, Cornell, Milliken Mills, Thornhill, Rouge River, Morgan Pool, Angus Glen — none reference Unionville). This is the same finding §15 already recorded, distinct from the completed header/location-pill fix (§9 item 2) — confirmed unrelated code paths. **Root cause reconfirmed precisely**: current geographic search logic works correctly across all 4 of its tiers (Municipality, Toronto-only District, Community Centre, Postal Code); Unionville is simply absent from that vocabulary — a data/coverage gap, not a search-logic defect. Remains **DEFERRED P2** — real product-design scoping work (which communities, which municipalities), not a v1 launch blocker. Not reopened, not implemented.

## 28. Production Physical-Device Regression QA — CLOSED / PASS / PRODUCTION PHYSICAL-DEVICE VERIFIED (2026-09-03)

**Status: CLOSED. Overall result: PASS.** Performed on a real physical iPhone against the live production deployment (`https://getdropin.vercel.app`), on `main` after the Feedback/Privacy CTA consistency patch (§27). A focused regression pass, not a full re-test of every previously-verified feature. **Zero launch-blocking regressions found.**

**Tests passed (6/6):**

1. **Homepage → Search → Results** — homepage, header, date, Near you, search field, quick-start chips, and activity cards all rendered correctly; "Badminton" search returned 19 real production results for the selected day; date/time-of-day/activity-chip/Nearest-first/list-grid controls, and the Happening now / Starting soon / Starting today groupings all rendered correctly; long names wrapped responsively; no overflow, overlap, stuck skeleton, or layout regression. "Updated yesterday" correctly reflected current refresh state, not treated as an issue.
2. **Activity Detail bottom sheet** — tested against a real session (Badminton, Toronto Pan Am Sports Centre, 875 Morningside Ave): sheet mechanics (dimming, drag handle, close, icon/title hierarchy), live status ("Happening now · Until 1:00 PM"), centre/address, age, walk-in info, Directions/Share CTAs, and the freshness/source footer all rendered correctly with no clipping or spacing regression — physically re-verifies the previously-completed mobile-sheet spacing/typography work against real production data.
3. **Directions** — provider chooser opened correctly, Apple Maps and Google Maps both presented; Google Maps opened and resolved to the correct named venue ("Toronto Pan Am Sports Centre"), not a raw coordinate — physically re-verifies §20's venue-aware Directions and provider chooser.
4. **Share** — real iOS native Share flow + iMessage: payload included correct time, venue, and address; URL appeared correctly. Moves §21's Share payload from implementation/stub verification to real production physical-device verification.
5. **About / Privacy / Feedback** — About: content, hierarchy, and footer all rendered correctly, long content scrolled correctly, including the physically-verified Feedback/Privacy CTA consistency fix (§27). Privacy: every disclosure section rendered correctly and scrolled fully, **including "Hosting & technical logs"** — this specifically moves §25's Security & Privacy Audit P1 fix from code/build-verified to **production physical-device verified**. Feedback: tapping "Email feedback" correctly invoked the real native iOS mail-app chooser (Gmail recognized) — confirmed a genuine `mailto:` flow, no fake submission state.
6. **Nearest first / Location** — correct selected state, real distance values (11/12/13/15/17 km) appeared on cards in both Happening now and Starting today groupings, nearest-first ordering held within each temporal group, long centre names continued wrapping correctly with distance added, no freeze/overlap/layout issue. (Location-permission-only-after-explicit-interaction was already verified in prior QA and is not reopened here, no regression found.)

**Three post-QA observations, recorded and explicitly classified — none are QA failures, none block this closure, none implemented here:**

1. **Feedback visual hierarchy** — *CLOSED / IMPLEMENTED, see §29.* Was: SMALL LAUNCH POLISH, decision pending. Both "Email feedback" and `feedback@getdropin.ca` currently render underlined, making the same action appear represented twice. Preferred future direction (not implemented): "Email feedback" stays the single primary CTA; the supporting email address stops being underlined and stays clearly secondary; mailto behavior/semantics unchanged; no section redesign.
2. **Directions provider chooser** — *CLOSED / IMPLEMENTED FOR V1, see §29.* Was: PRODUCT SIMPLIFICATION DECISION, decision pending. Real-device use raised the question of whether the chooser step (Directions → Apple/Google Maps → destination) is necessary for v1, versus a single direct Google Maps action better matching DropIn's "reduce thinking" philosophy. Not a functional bug — Directions works correctly today (Test 3, PASS). No implementation in this task.
3. **Share preview branding** — *LAUNCH BRAND/TRUST CONSIDERATION.* The current iMessage link preview shows a generic dark/grey card with the `getdropin.vercel.app` URL and no branded image — not a Share-function bug. Recorded for evaluation **after** custom-domain attachment: basic Open Graph/social metadata plus a branded DropIn preview image. Activity-specific share URLs / dynamic per-activity Open Graph cards recorded as a further, separate, **P2** item (also depends on the still-unresolved stable-identity gap already recorded in §21).

**Explicitly preserved, unchanged by this entry:** Geographic Search / Unionville remains **DEFERRED P2** (§27); the existing Security & Privacy Audit P2 backlog (§25) is unchanged; the existing Load/Concurrency P2 backlog (§26) is unchanged.

**Updated remaining launch sequence** (supersedes §7's stale diagram, which predates §13–§27 and still shows "Post-launch monitoring / QA" as the next step):

```
Production Physical-Device Regression QA          ✅ done (this section)
        ↓
Resolve post-QA launch decisions/polish items
explicitly approved by the product owner            ⬜ next
        ↓
Attach getdropin.ca
        ↓
Verify/update production-domain R2 CORS
        ↓
Inspect/update any production site URL
configuration still referencing getdropin.vercel.app
        ↓
Final custom-domain production smoke test +
focused physical-device spot-check
        ↓
LAUNCH
```

No step beyond "Production Physical-Device Regression QA ✅ done" has been executed as part of this entry.

## 29. Post-QA Launch Decisions — Feedback Visual Hierarchy + Directions Simplification (2026-09-03)

**Status: CLOSED. Both items implemented in one tightly scoped patch, resolving the two decision-pending observations recorded in §28.** `app/page.tsx` is the only application file changed. Typecheck, build, and lint all clean — lint at the existing 21-problem baseline, zero new findings.

**1. Feedback visual hierarchy — CLOSED / IMPLEMENTED.** "Email feedback" remains the single primary CTA (unchanged: bold, `underline`, `text-sage-text`). `feedback@getdropin.ca` is supporting information — the duplicate-underline competition flagged in §28 was removed by wrapping the address in its own `mailto:` link styled without an underline, `text-xs`, secondary color, so it no longer visually competes with the primary CTA. Root cause of the original underline (not previously diagnosed): iOS Safari's own data-detector auto-linkifies and underlines plain-text email addresses even without an explicit `<a>` tag — an explicit unstyled anchor defeats that. `mailto:feedback@getdropin.ca` behavior is unchanged; confirmed live (both the primary CTA and the supporting address resolve to the identical real mailto address).

**2. Directions provider chooser — CLOSED / IMPLEMENTED FOR V1.** The Apple Maps/Google Maps chooser introduced in §20 is removed. Directions now opens Google Maps directly, on every viewport, with a single tap — no intermediate Sheet, no `onClick` interception, no mobile/desktop branching. Removed: the chooser `Sheet` block, `mapChooserOpen` state, `appleMapsLinkRef`, `appleMapsUrl()`, and the Activity Detail sheet's stacked-sheet `onClose` guard (no longer needed with only one sheet ever open here). The visible CTA label remains exactly "Directions" — not renamed, no Google branding or icon added. Venue-aware destination logic is unchanged and preserved: `directionsQuery()`/`googleMapsUrl()` (same priority chain documented in §20 — centre+address+municipality, then centre+municipality, then municipality-only, coordinates only as a last resort) still drive the single Google Maps link. Live-verified: tapping Directions on a real Toronto session opened `https://www.google.com/maps/search/?api=1&query=Dufferin%20Grove%20Park%2C%20875%20Dufferin%20St%2C%20Toronto` directly in a new tab (`target="_blank" rel="noopener noreferrer"` preserved), with the Activity Detail sheet undisturbed underneath. Multi-provider choice and a saved map-app preference are not rejected outright — they remain a real **deferred P2**, to be revisited only if future user demand justifies the extra tap, per direct instruction.

**3. Privacy copy alignment — CLOSED.** Because Directions no longer offers a choice, §3's "Other websites" sentence no longer accurately said "your chosen map app" (as updated in §20). Now reads:

> "DropIn links to official municipal websites and to Google Maps for directions. Those services have their own privacy practices."

A repo-wide search confirmed no other user-facing copy anywhere in `app/`/`lib/` referenced "chosen map app," "Apple Maps," a "map provider chooser," or "provider selection" — this was the only place needing correction. No other About/Privacy content was touched.

**Explicitly not implemented in this entry:** Share/Open Graph preview branding remains **LAUNCH BRAND/TRUST CONSIDERATION** (§28 item 3), unchanged. No custom-domain (`getdropin.ca`) work, no R2 CORS work, no environment-variable changes. Production Physical-Device Regression QA (§28) remains **CLOSED / PASS**; Geographic Search / Unionville (§27) remains **DEFERRED P2**; the Security & Privacy Audit P2 backlog (§25) and Load/Concurrency P2 backlog (§26) are both unchanged.

## 30. Sheet Close-Button Persistent Focus Ring — IMPLEMENTED / PENDING PHYSICAL-DEVICE CONFIRMATION (2026-09-03)

**Status: PRE-LAUNCH UX/ACCESSIBILITY POLISH → IMPLEMENTED / PENDING PHYSICAL-DEVICE CONFIRMATION.** A post-§29-patch physical-device spot-check found a persistent sage circular focus ring around the About sheet's Close `×` on a touch-triggered open, while the same Close control opened from within About (the Privacy transition) showed no ring — a discrepancy despite both sheets sharing the identical `Sheet.tsx` close control. Root-cause investigation (documented in full in this session's own record) traced it to `Sheet.tsx`'s open-time programmatic autofocus (`(initialFocusRef?.current ?? closeButtonRef.current)?.focus()`, fired from a `useEffect` a task after the triggering pointer event) combined with WebKit's own `:focus-visible` heuristic, which can't always distinguish that detached script-driven focus from a genuine keyboard interaction — confirmed pre-existing (dates to commit `bec3127`, not introduced by `002aed0`/§29) and confirmed **not** a difference in implementation between About/Privacy/Activity Detail, which all share one `Sheet.tsx` close control with no per-consumer branching.

**What shipped:** `Sheet.tsx` now tracks real input modality (keyboard vs. pointer/touch) via two document-level listeners (`keydown`, `pointerdown`) — the only unambiguous signal every browser gives for this, not user-agent sniffing, not per-platform branching. When the open-time autofocus effect runs, it marks its own target with `data-autofocus-pointer` only when the triggering open was pointer/touch-driven (removed automatically on that element's next blur, so it never persists past the one ambiguous moment); a small, narrowly-scoped, unlayered rule in `globals.css` suppresses just that element's ring/outline for exactly that attribute+`:focus-visible` combination. No `focus-visible:`/`ring-*` utility class was removed from either the Close button or the Directions link (the app's other `initialFocusRef` consumer); no global focus-visible suppression; no `blur()`-after-focus hack; no UA sniffing; no iOS-only branching; no per-consumer About/Privacy special-casing. Focus is still moved into the sheet for every open, of every modality, preserving screen-reader/AT announcement of the newly-opened dialog.

**Verified live** (desktop Chrome, local dev server — physical iPhone re-verification explicitly deferred to the product owner per direct instruction): a pointer-triggered open of About, Privacy, and the Activity Detail sheet's Directions link all correctly land `data-autofocus-pointer` on the focused target with its ring/outline computed as `none`/`box-shadow: none`; a keyboard-modality open (a real trusted `keydown` preceding activation) correctly leaves the attribute unset and shows the genuine sage ring (`box-shadow: ... rgb(95, 117, 90) 0px 0px 0px 2px`, confirmed via computed style). Escape-to-close and click-to-close both re-verified working, unaffected. Typecheck, build, and lint all clean — lint unchanged at the existing 21-problem baseline.

**Explicitly not verified / not claimed:** Tab/Shift+Tab focus-trap behavior inside an open sheet could not be exercised through this session's browser-automation tooling (a CDP limitation with synthetic Tab-key default-action dispatch in the embedded tab, not a page behavior) — code inspection confirms `Sheet.tsx` implements no explicit Tab-trapping logic at all, a pre-existing characteristic entirely untouched by this fix, not something this entry adds or claims to fix. Also pre-existing and untouched: `Sheet.tsx` does not restore focus to the triggering element on close (observed directly: focus reverts to `<body>` after Escape) — not a regression from this fix, not in scope of the approved target behavior, and not corrected here. Physical iPhone confirmation of the ring fix itself remains the one explicitly outstanding item before this entry can move to CLOSED.

---

## Revision Notes

- 2026-08-21 — Initial launch-readiness plan recorded per explicit user request, ahead of any implementation. Cross-references `docs/ARCHITECTURE.md` (source-of-truth principle), `docs/PHASE_4_0_GEOSPATIAL_READINESS_MAP_NECESSITY_AUDIT.md` (Map View deferral), `docs/PHASE_3_3_DATA_REFRESH_SNAPSHOT_PIPELINE.md` (snapshot gitignore/architecture), and `docs/PRODUCT_PRINCIPLES.md` (existing transparency principles) rather than duplicating their content. Noted, as a factual finding rather than a fix: the current Feedback "Send" action has no backing API call and doesn't actually transmit anything, despite its confirmation copy — flagged for the pre-launch Feedback review in §2, not corrected here.
- 2026-08-24 — Added §10, recording a future optional "Support DropIn" contribution feature (PayPal candidate) as a not-yet-scoped idea, per explicit user request alongside a small, unrelated Privacy-sheet contact-link polish (`docs/LAUNCH_READINESS_1B_TRUST_PRIVACY_FEEDBACK_IMPLEMENTATION.md`). No implementation, no charity/tax-status claims, and a mandatory fresh privacy/security review before any future build.
- 2026-08-24 — Expanded §10 into a formal backlog item per explicit user request: goal, current direction, potential future UX, and a 7-point before-implementation review checklist, with the hard constraints (no "Donate"/charity framing, mandatory fresh privacy/security review, no expansion of the current Security & Deployment Audit scope) carried forward unchanged. Documentation only — no UI, dependency, third-party script, or Privacy/About copy was added or changed.
- 2026-08-25 — Added §11 (Launch Observability Strategy) and §12 (Future Backlog — Product Analytics), per explicit user request, as the final Phase 5A documentation item before Phase 5B. Records Cloudflare Web Analytics as the preferred launch-v1 basic-web-analytics option (subject to fresh verification at implementation time), the binding four-step Privacy-update requirement for whenever analytics is actually enabled, and the explicit deferral of detailed product analytics to its own future dedicated design phase. Also added a one-line cross-reference from §3.C to §11. Documentation only — analytics remains fully disabled (confirmed by direct inspection of `app/page.tsx`, unchanged), and the existing Privacy copy's "does not currently use analytics" claim remains accurate and was not touched.
- 2026-08-26 — Added §13, a required pre-launch gate for production load/concurrency verification, recorded immediately after all 7 municipalities' canonical data was live-migrated to R2 (Phase 5B-2B) — every verification performed through that phase was single-request/sequential, never concurrent, so this gate exists to make sure that gap isn't silently treated as "already covered." Documentation only, per explicit user request — no load-testing tooling was added or run.
- 2026-08-27 — Added §14, recording Aurora's ActiveCommunities "group fitness" data-completeness limitation as a tracked, non-blocking open issue, discovered during Phase 5B-3's first real production refresh run and diagnosed the same day. Aurora's own Completion Gate (Phase 3.6B) is correctly rejecting the incomplete upstream result; its last-known-good snapshot remains active and unaffected. Explicitly not marked resolved, and the Completion Gate itself must not be weakened or bypassed to force a green run. Documentation only, per explicit user request — no source/adapter code was changed.
- 2026-08-27 — Updated §7 to record the first real Vercel production deployment (`https://getdropin.vercel.app`) as LIVE VERIFIED — real smoke-tested production data, the exact read-only credential boundary, and the R2 CORS origin addition, full evidence in `PHASE_5B_PRODUCTION_INFRASTRUCTURE_PREFLIGHT.md` §10 and `PHASE_5B_RESPONSE_SIZE_ARCHITECTURE.md` §7. This is the first real instance of the "Production deployment" sequence step, not yet the full public launch — `getdropin.ca` remains disconnected and §9's mobile/physical-device verification requirement is still outstanding, now identified as the next step. Documentation only, per explicit user request — no application code, Vercel configuration, Cloudflare configuration, GitHub Actions, or domain attachment was touched.
- 2026-08-28 — Updated §9 to record that §9's outstanding mobile/physical-device verification requirement (flagged in the two entries above) has since had its first real pass: Round 1 real physical-iPhone QA against `https://getdropin.vercel.app` (commit `6120b0f`, "first physical-device QA"), which produced secondary-interaction-layer findings (not core-product-breaking) and a resulting Mobile UX Polish patch across `app/_components/Sheet.tsx`, `app/page.tsx`, `lib/dropin/activities.ts`, and `docs/PROJECT_WORKFLOW.md`, already build/typecheck/lint-clean and desktop/viewport-regression-clean. Documentation only, added ahead of Round 2 to make an explicit, previously-undocumented distinction: this patch is IMPLEMENTED + VIEWPORT VERIFIED but **not yet physically re-verified on a real iPhone** — Round 2 real-iPhone regression QA is recorded as the immediate next checkpoint and is explicitly not marked complete. No application code was changed as part of this entry.
- 2026-08-28 — Updated §9 and added §15, recording two new findings from a Round 2 real-iPhone QA pass, investigated the same day. (1) Searching "Unionville" (a real Markham community) was unrecognized — confirmed a genuine geographic-vocabulary/data gap, not a bug: DropIn has no informal-community concept for any municipality, and the string doesn't exist anywhere in Markham's canonical data. Recorded as new §15, explicitly non-blocking, deliberately deferred to its own future product-design task — not implemented, not aliased, not worked around here, matching the same treatment this document already gives the Aurora issue (§14). (2) A facility search (e.g. "Oakridge") could replace the header pill's "Near me" text with the full Community Centre name, which could wrap across two lines on a real phone and misrepresented the pill's area/proximity meaning — traced to `locationPillLabel()` in `app/page.tsx` rendering any resolved location's label unconditionally. **This one was fixed**, same day: `type: "centre"` is now excluded from the pill's display logic; Community Centre search continues to filter results exactly as before, and user geolocation/distance calculation/`sessionMatchesLocation` are entirely untouched. `docs/SEARCH_ENGINE.md` and `docs/INFORMATION_ARCHITECTURE.md` updated in the same pass to state the Community-Centre-filters-but-doesn't-drive-the-pill rule explicitly. Build, typecheck, and lint (zero new errors — 10 pre-existing, unrelated errors confirmed identical on unmodified `main`) all clean.
- 2026-08-28 — Added §16, recording a missing error state for a failed `/api/sessions` fetch, discovered incidentally while investigating a real-iPhone Preview-deployment data-loading failure (the Mobile Sheet refinement's Preview branch, §9). The actual cause of that specific failure was a missing R2 CORS origin for the Preview URL, confirmed directly (an `OPTIONS` preflight against the presigned R2 URL with the Preview origin returned `403`; a `GET` with the same origin returned `200` but with no `Access-Control-Allow-Origin` header, the exact signature of a browser-side CORS block) — production's own CORS entry and data path were re-confirmed working and untouched throughout. Separately, the investigation surfaced that `app/page.tsx`'s session-fetch `useEffect` has no `.catch()` at all, so ANY fetch failure (not just this CORS gap) leaves `loading` stuck `true` and `sessions` stuck empty indefinitely, with no error message and no retry — recorded as its own non-blocking, deferred finding, explicitly not fixed as part of this investigation. No application code was changed as part of either finding in this entry — documentation only.
- 2026-08-28 — Updated §16 to record that the R2 CORS gap it documents was resolved by the project owner (append-only addition of the exact Preview origin to `AllowedOrigins`, nothing else in the policy changed) and independently re-verified (fresh preflight + GET against the Preview origin now return the correct `Access-Control-Allow-Origin`, matching production's already-working behavior). Updated §9 item 3 to record the Mobile Sheet spacing/title refinement as **PHYSICALLY VERIFIED on a real iPhone**, once the CORS fix unblocked that Preview deployment's data loading — resolving this section's own previously-open "requires real-iPhone verification" flag for that specific item. Added §9 item 4, recording a separate shared-Sheet close-button finding from the same physical QA pass: `focus-visible:ring-2` (pre-existing, unrelated to this refinement) painting the full 44×44 touch target as a large visible circle, root-caused to Round 1's earlier touch-target enlargement rather than anything in this branch. **Fixed** the same day — the outer 44×44 element is now pure interactive hit-target (no visible chrome), with a small inner `<span>` carrying the actual background/ring, wired via Tailwind's `group` mechanism, so the visible ring now traces ~28px instead of the full 44px box at both breakpoints; touch target, keyboard focus visibility, accessible name, and desktop sizing all unchanged, confirmed structurally and via build/typecheck/lint (zero new findings). This specific fix is **not yet physically verified** — flagged as its own open item. About's information-density problem remains untouched and open throughout all of the above, per this document's existing §9/§15 framing.
- 2026-08-28 — Updated §9 item 3 to record that the Newmarket title-normalization work is now physically verified on a real iPhone Preview (materially cleaner). Added §17: the Newmarket facility-suffix normalization fix and its full-dataset regression evidence; the Richmond Hill adult/teen case audited and deliberately left unchanged (real contradicting evidence found elsewhere in the same tables); a minor `"Pickleball-Open Play"` hyphen-spacing follow-up recorded, not patched; and the Activity Icon Consistency resolution — the Activity Detail title's conditional sport icon is removed (text-only title, `[Activity title] [Close]`), while homepage quick-start icons and Results cards' text-first design are both explicitly unchanged, per the "icons support discovery, not identification" product decision. Activity Detail title typography remains untouched, deferred to the upcoming Mobile Sheet system task. Typecheck, build, and lint all clean (lint: zero new findings against the existing baseline). Not committed, not pushed, not deployed — implemented locally on the `preview/mobile-sheet-refinement` branch, pending review.
- 2026-08-30 — Closed §14: Aurora ActiveCommunities Data Completeness marked **CLOSED / LIVE VERIFIED**, on branch `fix/aurora-centre-partition-fallback`. Root cause was a real 20-result page cap on `activities/list` with no working pagination (re-confirmed exhaustively). Fixed via evidence-based centre discovery (`activities/map`, discovery-only), sequential per-centre `activities/list` retrieval (concurrent requests were found live to be intermittently unreliable against Aurora's session-scoped backend), a one-bounded-retry guard for a separately-discovered contradictory `total_records > 0`/`items: []` upstream response (fails closed if the retry repeats the contradiction), and a merged-total/duplicate-id cross-check against the original unfiltered `total_records` — the original Completion Gate itself was never weakened, only preserved and extended. Live-verified: 10/10 repeated Aurora fetches clean post-fix, 45 raw keyword calls (44 clean, 1 correct fail-closed throw), a real 7-municipality production R2 refresh (7/7, Aurora activating a genuinely fresh 511-session snapshot), and the actual Daily Production Data Refresh GitHub Actions workflow (Run #6, this branch) — SUCCESS, combined object written and read-back verified at 44,393 sessions. Added §18, recording two small, unrelated, non-blocking findings from that same GitHub run (a job-summary JSON-parsing display gap, and a Node.js 20 deprecation notice) — explicitly not fixed here. No application code changed as part of this documentation entry.
- 2026-08-31 — Added §19, recording the Mobile Sheet System's Prompt 4A (About/Privacy information-architecture audit) and Prompt 4B (refinement) outcome: both surfaces remain shared-Sheet consumers for v1, not dedicated routes — confirmed by inspecting actual content length and the app's existing (routerless) architecture, not assumed. Prompt 4A's own recommendation to add `?info=about`/`?info=privacy` URL sync was explicitly deferred as unnecessary launch-stabilization complexity, per direct instruction — not implemented. Prompt 4B found the existing About/Privacy markup already satisfied every stated hierarchy/spacing/typography requirement (product of the earlier Mobile UX Polish pass, §9/§16/§17) and made no JSX or spacing changes to either sheet; `app/_components/Sheet.tsx` was not touched. Live-verified in-browser: heading hierarchy, the About → Privacy transition (including its brief, self-resolving crossfade), state preservation on close, and desktop centered-modal presentation. Sub-768px physical-iPhone verification remains outstanding, same tooling limitation already recorded for Prompt 3. Small factual-status corrections made in the same pass: §3's "not yet written" framing corrected to reflect that Privacy has in fact been written and implemented as a Sheet for some time; light cross-references from §1/§3 to this section added. No other historical entries were rewritten.
- 2026-08-30 — Reconciled `preview/mobile-sheet-refinement` and `fix/aurora-centre-partition-fallback` via a real merge commit, per explicit user approval. `lib/dropin/sources/activecommunities/client.ts` and `index.ts` conflicts resolved by taking the Aurora branch's versions entirely (confirmed byte-identical afterward). This document's conflicts resolved by hand: all Preview QA content (§9, §15, §16, §17) preserved unchanged; Aurora's closed §14 (already merged automatically, no conflict) and its Infrastructure Maintenance Backlog renumbered to §18 to avoid colliding with Preview's own §16; both branches' revision notes preserved in full, Aurora's appended after Preview's rather than interleaved. No other application code changed as part of this merge.
- 2026-08-31 — Added §20, recording POST-QA Polish 01's Directions map-provider chooser (investigated first, then implemented per explicit approval): `directionsUrl()` generalized into a shared `directionsQuery()` plus per-provider `googleMapsUrl()`/`appleMapsUrl()` builders (same underlying destination data, same existing priority chain — centre+address+municipality, then centre+municipality, then municipality-only); on mobile viewports only, Directions now opens a small secondary Sheet (Apple Maps / Google Maps) instead of navigating immediately, reusing `Sheet.tsx` exactly as-is; desktop's one-click Google Maps behavior is untouched. Investigation found real, live production evidence for the priority chain (100% centre coverage; Toronto/Vaughan/Markham/Newmarket ~100% address coverage; Mississauga/Richmond Hill/Aurora 0% address; 1,642 sessions, 3.7%, with neither address nor coordinates) and 42 `centre: "Virtual"` Aurora sessions where Directions is semantically meaningless — both recorded, neither fixed here. Waze and provider-preference persistence deliberately deferred, not implemented. Privacy's "Other websites" wording (§3) updated in the same change to provider-neutral language ("your chosen map app"), per this document's own same-change-as-the-behavior discipline. Implemented and verified on `preview/mobile-sheet-refinement` only; physical-iPhone re-verification still outstanding.
- 2026-08-31 — Updated §20 to **CLOSED / PHYSICALLY VERIFIED**, recording a real-iPhone QA pass: mobile provider chooser, Apple Maps, and Google Maps all PASS on a Toronto venue with full destination data (resolved as the correct named place on both providers, not a raw pin); the missing-address fallback specifically PASS on a real affected session — "Fun Swim" at DropIn centre value "Lions Club of CV Main + Leisure Pool", Mississauga (no address, no coordinates in the canonical data) — Google Maps still resolved it correctly to "Lions Club of Credit Valley Outdoor Pool". Conclusion recorded: Directions should not be hidden merely for missing address/coordinates, since the centre+municipality fallback is a genuinely useful provider-level place search, confirmed on one real case — explicitly not treated as proof every one of the 1,642 affected sessions resolves this well; that broader data-quality gap remains open. Waze, provider-preference persistence, the Mississauga/Richmond Hill/Aurora address-data gap, and the 42 virtual-session cases all remain deferred/unresolved backlog items, none of them blockers to this closure. Documentation only — no application code changed as part of this entry.
- 2026-09-02 — Added §21, recording POST-QA Polish 02B's Share payload polish: `handleShare()`'s `navigator.share` call now gives `title`/`text`/`url` distinct roles instead of one duplicated blob, address included only when the session has one; the clipboard fallback is a separate, self-contained string (includes the activity name inline, since it gets no separate title treatment). Verified live with stubbed `navigator.share`/`clipboard.writeText` against both an address-present and an address-absent real session — correct output both times, no location/id/infrastructure leakage in either payload. Per-activity deep linking remains explicitly deferred: a dedicated investigation (POST-QA Polish 02) found session `id` genuinely unstable across two real consecutive daily refreshes for Toronto (2.31%) and Mississauga (0.63%) specifically — together ~91% of all sessions — and found `sourceScheduleId` (the prior untested hope for a more stable id) only actually helps for 5 of 7 municipalities, not those two, while also not currently reaching the client at all (`TrimmedSession` strips it). Tracked as a post-launch product/architecture item, not scheduled to a phase. Shared URL remains the DropIn homepage constant, unchanged. Typecheck/build/lint clean against the existing 21-problem baseline. Implemented and verified on `preview/mobile-sheet-refinement` only.
- 2026-09-02 — Added §22, recording the Vaughan PerfectMind pagination-density incident and its resolution: `lib/dropin/sources/perfectmind/client.ts` now requests a rolling 30-day forward horizon (computed once per pull from the refresh's own "today," shared by Vaughan/Markham/Newmarket) instead of a hardcoded ~2027-06-30 bound — a deliberate product-scope decision (7-day UI commitment, generous operational headroom), not a pagination workaround. `MAX_PAGES_PER_CATEGORY` (40) and the Completion Gate (`perfectmind/index.ts`) are both unchanged; failed municipalities still preserve their previous known-good snapshot. Live-verified against real production code: Vaughan/Fitness Centre (the failing category) now completes at 23/40 pages with real headroom, all of Vaughan/Markham/Newmarket's categories complete correctly with data spanning exactly today through 30 days out, and a real single-municipality Vaughan refresh activated a fresh 1,610-session snapshot end-to-end. Separately fixed the GitHub Actions summary-parser issue recorded non-blocking in §18: `.github/workflows/daily-refresh.yml` now invokes `npx tsx scripts/refresh/index.ts` directly instead of `npm run`, whose command banner was confirmed (by direct reproduction) to break `format-summary.ts`'s `JSON.parse`; `format-summary.ts` itself was not changed. Verified live: clean JSON capture, correct summary rendering, and a synthetic mixed-result payload confirming failures still render accurately, not hidden. Typecheck/build/lint clean against the existing 21-problem baseline. Implemented and verified on `preview/mobile-sheet-refinement` only.
- 2026-09-02 — Added §23, recording the one-time Newmarket canonical-snapshot baseline transition, made necessary by §22's approved horizon migration: the real first production Newmarket refresh under the new 30-day horizon (484 sessions) was correctly rejected by the unmodified `>50%` count-collapse gate against its old unbounded-horizon baseline (2,064 sessions) — investigated and proven unable to self-resolve (a rejected refresh never updates the comparison baseline, so every future 30-day fetch would keep failing against the same stale number indefinitely). Resolved with a real, fully-verified Newmarket fetch (all 6 categories complete, 2–5 pages each, well under the cap) promoted directly to `canonical/newmarket/latest.json` via the project's existing `SnapshotStorage.writeAtomic` primitive — no code changed, no gate weakened, no Newmarket-specific exception added. A second, completely normal refresh through the standard fully-gated pipeline immediately afterward succeeded on its own (no bypass, no special handling), proving the system now compares 30-day-to-30-day baselines correctly and Newmarket is back on the fully autonomous daily-refresh path. Combined object rebuilt and verified; Vaughan, Markham, and Aurora confirmed unchanged throughout.
- 2026-09-02 — Added §24, closing the Production Daily Refresh / canonical snapshot / failure-isolation infrastructure milestone as **CLOSED / END-TO-END PRODUCTION VERIFIED**, on the real, scheduled Daily Production Data Refresh Run #13: workflow SUCCESS, 7/7 municipalities refreshed (Toronto 24,376 · Mississauga 16,836 · Richmond Hill 216 · Aurora 485 · Vaughan 1,604 · Markham 278 · Newmarket 484), combined canonical object 44,279 sessions / 26.34 MB, read-back verification PASS — the first clean 7/7 run following §22's PerfectMind horizon fix and §23's Newmarket baseline transition. Recorded as the closing confirmation that Vaughan's new 30-day horizon, Newmarket's baseline migration, and Aurora's self-recovered transient failure all held under a fully autonomous scheduled run, and that failure isolation (previous known-good snapshot preserved on individual-municipality failure) is proven by the preceding runs' own behavior, not merely designed-for. Node.js 20 deprecation (§18) and combined-payload-size/load-concurrency verification (§13) remain explicitly open, non-blocking follow-ups, unaffected by this closure. Documentation only — no application code, refresh logic, or workflow configuration changed; no snapshots modified; no refresh triggered as part of this entry.
- 2026-09-02 — Added §25, closing the Production Security & Privacy Readiness Audit (§5) as **CLOSED / PRE-LAUNCH VERIFIED**: no P0 findings; one P1 finding (Privacy copy silent on standard hosting-provider request logging) resolved in the same pass with a new, provider-neutral "Hosting & technical logs" Privacy section (`app/page.tsx`) — no other Privacy claim changed. R2 read/write credential separation, the absence of any public write path, absence of client-exposed secrets, explicit-action-only geolocation, inactive analytics, and mailto-only Feedback were all independently verified against the real production configuration and codebase, not assumed. `/design/*` public reachability and the 4 `npm audit` high-severity findings (all confirmed to have no real trigger path in this app's runtime — no `next/image` usage, postcss build-time-only) remain recorded as P2, not addressed here. Documentation and one Privacy-copy addition only — no R2/credential/workflow/API configuration changed, no dependencies updated, no security headers added, no `/design` routes modified.
- 2026-09-03 — Added §26, closing the Production Load / Concurrency Verification gate (§13) as **CLOSED / PRODUCTION VERIFIED**: real, read-only load run against `https://getdropin.vercel.app`'s actual session-data path (not `/` alone) — a 5-request sequential baseline plus four concurrency stages (5/10/20/30 concurrent real clients, the last run since 5–20 were clearly healthy), 65 total real requests, 100% success, zero failures/timeouts, identical 33,276,633-byte payload every single time. Architecture trace confirmed `/api/sessions` never holds the payload itself — it redirects to a presigned R2 URL generated by a local signing operation, and the actual object is served directly by Cloudflare R2/CDN, bypassing Vercel's function body/bandwidth entirely; TTFB stayed flat and healthy (median 0.45–0.62s) across all concurrency levels, with total-transfer-time growth attributed to this test's own shared local bandwidth, not server-side degradation. The combined payload (measured live at ~31.7 MB, updated from the previously-quoted stale "~26 MB" figure) was classified **D — effectively mitigated by the current delivery architecture** for this launch's realistic traffic shape, with real, separately-assessed dimensions (server scalability, bandwidth, user-perceived performance, mobile-network implications) rather than a single verdict. No P0/P1 findings; remaining P2s (no cross-visit payload caching due to unique-per-request presigned URLs, no download-progress indicator, §16's already-tracked missing `.catch()`) recorded, not fixed. Documentation only — no code, configuration, snapshots, or workflows changed; no refresh triggered; no deployment made.
- 2026-09-03 — Added §27, reconciling two pre-launch backlog items ahead of the final Production Physical-Device Regression QA. Feedback/Privacy CTA consistency (not previously tracked under this name) investigated fresh and closed: the Privacy Sheet's in-app CTA button (`app/page.tsx`) was missing `font-medium`, rendering visibly thinner than the Feedback CTA's `<a href="mailto:...">` link — confirmed live in-browser, fixed with the one-class addition this needed, no wording/layout/navigation/semantics change (the two remain intentionally different element types). Geographic Search / Unionville re-tested live against real current production data (44,279 sessions, real `parseQuery` calls) — Markham/Vaughan/North York/Scarborough all resolve correctly today; "Unionville" still resolves to nothing, confirmed absent from every real centre name, address, and district field in current data. Reconfirmed as a genuine geographic-vocabulary/data-coverage gap, not a search-logic defect (all 4 existing location tiers work correctly) and not the same issue as the already-completed header/location-pill fix (§9 item 2). Remains **DEFERRED P2** — not reopened, not implemented, correctly not a v1 launch blocker. Typecheck/build/lint clean against the existing 21-problem baseline.
- 2026-09-03 — Added §28, recording the Production Physical-Device Regression QA as **CLOSED / PASS / PRODUCTION PHYSICAL-DEVICE VERIFIED**: a real physical iPhone, tested directly against the live `https://getdropin.vercel.app` deployment on `main` after §27's CTA patch. All 6 tests passed (Homepage → Search → Results; Activity Detail; Directions; Share; About/Privacy/Feedback; Nearest first/Location) with zero launch-blocking regressions — this run specifically moves §20's Directions/provider-chooser work, §21's Share payload, §25's "Hosting & technical logs" Privacy fix, and §27's CTA fix from code/build/preview-level verification to real production physical-device verification for the first time. Three post-QA observations recorded, explicitly classified as non-blocking and not implemented here: Feedback visual hierarchy (small launch polish, decision pending), the Directions provider-chooser's necessity for v1 (product simplification decision, decision pending), and Share's iMessage link-preview branding (launch brand/trust consideration, with dynamic per-activity Open Graph previews recorded as a further P2). Geographic Search / Unionville (§27), the Security & Privacy Audit P2 backlog (§25), and the Load/Concurrency P2 backlog (§26) are all explicitly unchanged by this entry. §7's stale sequence diagram was given a one-line pointer to this section rather than rewritten; §28 itself carries the current, accurate remaining launch sequence (post-QA decisions → attach `getdropin.ca` → verify/update R2 CORS → inspect any remaining `getdropin.vercel.app` references → final custom-domain smoke test + spot-check → launch), none of which was executed as part of this entry. Documentation only — no application code, R2/CORS configuration, environment variables, or application behavior changed.
- 2026-09-03 — Added §29, closing both post-QA decisions §28 left pending. Feedback visual hierarchy: **CLOSED / IMPLEMENTED** — the supporting `feedback@getdropin.ca` address no longer renders underlined (root cause was iOS Safari's own data-detector auto-linkification, not DropIn CSS), "Email feedback" remains the single primary CTA, mailto behavior unchanged. Directions provider chooser: **CLOSED / IMPLEMENTED FOR V1** — the §20 Apple Maps/Google Maps chooser is removed; Directions now opens Google Maps directly on every viewport with the label unchanged ("Directions", no branding added); the venue-aware `directionsQuery()`/`googleMapsUrl()` destination logic is unchanged and live-verified against a real Toronto session; multi-provider choice/saved preference remain a real deferred P2, not rejected. Privacy's "Other websites" sentence (§3, previously updated to "your chosen map app" in §20) now explicitly names Google Maps, aligned with actual v1 behavior — the only user-facing copy found needing correction after a repo-wide search. §28's observation classifications and §20's chooser description were both updated in place with pointers to this section rather than rewritten. Share preview branding (§28 item 3) remains unimplemented, unchanged. Geographic Search/Unionville (§27), the Security P2 backlog (§25), and the Load/Concurrency P2 backlog (§26) are all unchanged. `app/page.tsx` was the only application file changed; typecheck/build/lint all clean at the existing 21-problem baseline. No custom-domain, R2 CORS, environment-variable, or Share/Open Graph work performed.
- 2026-09-03 — Added §30, closing (pending physical-device confirmation) a persistent sage focus ring found around the About sheet's Close `×` during a post-§29 physical-device spot-check. Root-caused via a dedicated investigation, first performed as an investigation-only pass with no code changes, then approved for a scoped fix: `Sheet.tsx`'s open-time programmatic autofocus, run from a `useEffect` detached from the triggering pointer event, can trip WebKit's own `:focus-visible` heuristic into treating a touch-triggered open as keyboard-equivalent — pre-existing since commit `bec3127`, not introduced by `002aed0`/§29, and identical across About/Privacy/Activity Detail since all three share one `Sheet.tsx` close control. Fixed via a small shared input-modality tracker (real `keydown`/`pointerdown` listeners, not user-agent sniffing) that marks the open-time autofocus target with `data-autofocus-pointer` only for pointer/touch-triggered opens (cleared on next blur), paired with one narrowly-scoped `globals.css` rule suppressing the ring/outline for exactly that attribute+`:focus-visible` combination — no ring/focus-visible utility classes removed, no global suppression, no per-consumer branching, no UA sniffing. Live-verified on desktop Chrome: pointer-triggered opens of About, Privacy, and Activity Detail's Directions link all correctly suppress the ring; a keyboard-modality open correctly still shows it (verified via computed `box-shadow`); Escape-to-close and click-to-close unaffected. Typecheck/build/lint clean at the existing 21-problem baseline. Two pre-existing, unrelated `Sheet.tsx` gaps were surfaced but explicitly left untouched, out of scope for this fix: no Tab-key focus trap exists inside an open sheet, and focus is not restored to the triggering element on close. Physical iPhone re-verification of the ring fix itself is the one remaining item before this entry moves to CLOSED, to be performed separately by the product owner. Production Physical-Device Regression QA (§28) status is unchanged.
